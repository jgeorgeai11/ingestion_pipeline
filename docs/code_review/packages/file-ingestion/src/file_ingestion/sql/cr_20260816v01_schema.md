---
name: cr_20260816v01_schema
goal: Address code quality issues identified in code/file_ingestion/sql/schema.sql to align with sql-development best-practices and DDL correctness, re-reviewed after the CHECK-constraint and COMMENT ON additions.
created: 2026-08-16 19:29:01
updated: 2026-08-17 09:44:03
---

## Implementation Plan

1. [completed] Formatting — line length - `code/file_ingestion/sql/schema.sql`
   - 1.1. [minor] Line 65: The `document` table comment literal is 174 characters on one line, exceeding the 100-character limit (best-practices §7). PostgreSQL concatenates string constants separated by whitespace containing a newline, so the literal can be wrapped without changing the stored comment.
        - Current: `    'One row per ingested document, identified by collection_path (an ltree). Holds title (human-readable reference), n_parsed_sections, source_binary_hash, and ingested_at';`
        - Expected:
          ```sql
          comment on table {schema_name}.{document_table} is
              'One row per ingested document, identified by collection_path '
              '(an ltree). Holds title (human-readable reference), '
              'n_parsed_sections, source_binary_hash, and ingested_at';
          ```
        - Resolution: Implemented as specified — wrapped into three adjacent string constants, no line now exceeding 100 characters. Added a short comment above both wrapped literals explaining the PostgreSQL adjacent-string-constant concatenation the wrapping relies on, so a future editor does not "fix" it back. Verified against live PostgreSQL: rendering the template into a throwaway schema and reading `obj_description` back returns the original single-line text byte for byte.
   - 1.2. [minor] Line 68: The `document_content` table comment literal is 236 characters on one line — the longest line in the file and more than twice the 100-character limit (best-practices §7). Wrap it the same way as 1.1.
        - Current: `    'Section-level text of each document — one row per section keyed by (collection_path, sort_order), with heading_text, content_text, word_count, and page_start/page_end provenance. Use for reading or keyword-filtering document text';`
        - Expected:
          ```sql
          comment on table {schema_name}.{content_table} is
              'Section-level text of each document — one row per section keyed '
              'by (collection_path, sort_order), with heading_text, '
              'content_text, word_count, and page_start/page_end provenance. '
              'Use for reading or keyword-filtering document text';
          ```
        - Resolution: Implemented as specified — wrapped into four adjacent string constants (longest line now 79 characters, down from 236). The em dash stays in the first fragment; the live-database read-back in 1.1 confirms the stored description is unchanged and still carries it (see also 4.1).

2. [completed] Document the `[load].schema_comment` override in the template note (parity proposal rejected — see 2.1) - `code/file_ingestion/sql/schema.sql`
   - 2.1. [minor] Lines 13, 59-68: The template comments both tables but never the schema, while `_utils.ensure_schema` accepts a `schema_comment` override alongside the two table overrides. The result is asymmetric: with no config override the tables still carry a generic description, but the schema's `obj_description` is NULL, so the MCP `list_tables` surface has nothing to show for the schema itself. The note on lines 61-63 also enumerates only `[load].document_table_comment / content_table_comment`, omitting `[load].schema_comment`, so the file under-documents the mechanism it describes. See `cr_20260816v01__utils.md` (2.2).
        - Current: the lines 61-63 note enumerates only `[load].document_table_comment / content_table_comment`, omitting `[load].schema_comment`
        - Expected: extend the lines 61-63 note to name `[load].schema_comment` — documentation only. Scope amended 2026-08-17: the originally proposed `comment on schema` addition is rejected, because `config/test/ingest_test_document.toml` runs this template against the live `cms_iom` schema with no override keys, so a baked-in generic schema comment would overwrite the production schema description on every test run; the template/config asymmetry is deliberate (full rationale in `cr_20260816v01__utils.md` 2.2)
        - Resolution: Implemented as specified (documentation only) — extended the note above the `comment on table` statements with a paragraph stating that the schema is deliberately not commented here, that schema descriptions come only from `[load].schema_comment`, and why (a schema description has no sensible generic text, and configs that load into a shared pre-existing schema would have its description overwritten on every run). No `comment on schema` was added, per the amended scope; the live-database check for 1.1 confirms the rendered template leaves the schema's `obj_description` NULL.
   - 2.2. [suggestion] Lines 64-68: Both table comments enumerate their columns in prose, which is what forces the over-long literals in findings 1.1/1.2 and duplicates information the catalog can hold per column. `comment on column` statements would keep each line short and let `col_description` surface the same detail column by column.
        - Current: column names listed inside the table-level comment strings
        - Expected: keep a one-sentence table comment and move the per-column detail to `comment on column {schema_name}.{document_table}.source_binary_hash is '...'` (and similar)
        - Resolution: Deferred — the consuming MCP `list_tables` tool reads table-level `obj_description` only, so column comments would not surface today; revisit if a column-level catalog reader is added. Re-decided 2026-08-21 in `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.2): still deferred — the rebuild window changes nothing here because `comment on` statements run unconditionally on every ingest anyway (they are not blocked by `create table if not exists`), so column comments can land whenever a column-level consumer exists, with no migration cost.

3. [completed] Idempotency has no migration path - `code/file_ingestion/sql/schema.sql`
   - 3.1. [minor] Lines 19, 40: `create table if not exists` makes re-runs safe but turns them into a complete no-op for any pre-existing table. Every constraint added after first deployment — the seven CHECK constraints on lines 23, 30-31, 45, 49, 50, 51, 54, 56 — silently never reaches an environment whose `document`/`document_content` tables predate them, so the DDL and the live schema can disagree with no error. Nothing in this template or in `ensure_schema` reconciles the difference. The `comment on` statements below run unconditionally, which makes the gap easy to miss: comments refresh on every run, constraints do not.
        - Current: `create table if not exists {schema_name}.{document_table} (...)` — additive changes never applied to existing tables
        - Expected: at minimum a header note stating that constraint/column additions require a manual `alter table` migration; better, idempotent `do $$ ... $$` blocks that add each constraint when absent
        - Resolution: Implemented at the documented minimum, not the "better" option — added a "Migration caveat" paragraph to the header block stating that the `if not exists` creates make a re-run a complete no-op for existing tables, that anything added later (constraint, column, default) must be applied by a manual `alter table` in each deployed environment, and that the unconditional `comment on` statements at the bottom are the exception, so refreshed comments are not evidence that constraints are in sync. Deviation and rationale: the `do $$ ... $$` reconciliation blocks were not added — they would need one guarded block per constraint with `pg_constraint` lookups against system-generated names for the seven unnamed CHECKs (the template names none of them), which is a schema-migration design decision with its own naming and rollout concerns rather than a review fix, and it cannot be validated here beyond a throwaway schema. The note makes the gap explicit so it is no longer silent; converting to generated migrations remains available as a follow-up activity.

4. [completed] Encoding contract - `code/file_ingestion/sql/schema.sql`
   - 4.1. [minor] Line 68: The comment text contains an em dash, which is the sole reason `_utils.ensure_schema` reads this file with an explicit `encoding="utf-8"` (its lines 85-87). Nothing pins that pairing: `unit_tests/test_utils.py::TestEnsureSchemaComments::test_real_template_renders_table_comments` renders this exact file but asserts only ASCII substrings, so dropping the encoding argument would mojibake this line on a cp1252 default with the suite still green. See `cr_20260816v01__utils.md` (2.1) and `cr_20260816v01_test_utils.md` (2.3).
        - Current: `'Section-level text of each document — one row per section keyed by ...'` with no regression guard
        - Expected: keep the em dash and add the assertion in `test_utils.py` that it survives rendering (alternatively, replace it with an ASCII hyphen and drop the hidden dependency)
        - Resolution: Implemented as specified via the first option — the em dash is kept (now in the wrapped literal's first fragment, see 1.2) and `test_utils.py::TestEnsureSchemaComments::test_ensure_schema_real_template_renders_table_comments` asserts `"—" in executed`. Confirmed the pairing is now pinned: this host's preferred encoding is cp1252 and a platform-default `read_text()` of this file does not preserve the character, so dropping `encoding="utf-8"` from `ensure_schema` fails the new assertion. Paired findings: `cr_20260816v01__utils.md` 2.1, `cr_20260816v01_test_utils.md` 2.3.

5. [completed] Optional hardening - `code/file_ingestion/sql/schema.sql`
   - 5.1. [suggestion] Lines 40-57, 68: The content table's own comment advertises it as the place to "read or keyword-filter document text", but `content_text` and `heading_text` have no supporting index — no GIN/`tsvector`, no `pg_trgm`. Every keyword filter is a sequential scan over the full corpus text.
        - Current: no index on `heading_text` / `content_text`
        - Expected: `create index if not exists {content_table}_content_fts_idx on {schema_name}.{content_table} using gin (to_tsvector('english', coalesce(content_text, '')));`
        - Resolution: Deferred — corpus size and the actual query mix are not established here, and an FTS index changes ingest write cost; add it when a measured keyword-filter workload exists rather than speculatively. Re-decided 2026-08-21 in `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.2): still deferred, and the rebuild window is NOT the cheapest moment for this one — a `create index if not exists` added to the template executes on every run and reaches pre-existing tables (the migration caveat blocks only `create table` internals), so an index can land the moment a measured workload exists, with no rebuild needed. The keyword workload today flows through the embedding tables' `chunk_tsv` GIN index (hybrid retrieval); the content table's keyword use remains incidental.
   - 5.2. [suggestion] Lines 21, 46-47: `title`, `heading_text`, and `content_text` are unconstrained `text`, so an empty string `''` is accepted and is distinct from NULL. Notably, `''` would satisfy the both-null guard on line 54, letting a section with neither real heading nor real content pass every DB check.
        - Current: `title text not null`, `heading_text text`, `content_text text`
        - Expected: `check (nullif(trim(heading_text), '') is not null or nullif(trim(content_text), '') is not null)` and `check (trim(title) <> '')`
        - Resolution: Deferred — carried over from the 2026-06-22 review and still conditional on whether the load step can emit `''`; the Pydantic models are the primary guard and no blank-string row has been observed. Raise to a CHECK only if a blank value is ever written. IMPLEMENTED 2026-08-21 by `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.1): the corpus rebuild made template DDL additions reachable, so `check (trim(title) <> '')` and the strengthened not-both-empty section guard (`nullif(trim(...), '')` on heading/content) landed in `schema.sql`, together with the symmetric title check in `excel_schema.sql` (preserving the schema symmetry that was the stated reason for deferring each alone). Verified against live PostgreSQL by real-database tests (blank title and both-blank section rejected by the DB, heading-only sections still accepted); zero stored rows violate the new checks (verified 2026-08-20 across 250 documents / 261,022 content rows).

## Skills with No Issues

1. best-practices §1-6 (explicit columns, joins, group/order by, union, NULL handling, CTEs): N/A — this is a DDL template with no SELECT, join, or CTE.
2. best-practices §7 (Formatting — lowercase, 4-space indent, ≤100 chars): **Issues found** — see 1.1 and 1.2. Lowercase and 4-space indentation are correct throughout; only the two comment literals exceed the length limit.
3. best-practices §8 (Comments — explain why, above the line, kept current): No issues found. The header block (lines 1-9), the invariant-mirroring notes (lines 16-18, 36-39), the `source_binary_hash` sizing rationale (lines 25-28), and the override-precedence note (lines 59-63) all explain why and sit above the code they describe.
4. best-practices §9 (Query block annotation): N/A — no query blocks or CTEs.
5. DDL correctness — ltree provisioning: No issues found. `create extension if not exists ltree;` precedes the first use of the type and is idempotent.
6. DDL correctness — PK/FK design: No issues found. `collection_path` as the `document` PK, the composite `(collection_path, sort_order)` PK on `document_content`, and the `on delete cascade` FK correctly model the one-to-many grain; the child PK's leading column backs the FK, so no separate index is needed.
7. DDL correctness — type sizing: No issues found. `numeric(20,0)` spans the full unsigned 64-bit range `[0, 2^64)` that `bigint` would overflow, and `timestamptz` is the right choice for `ingested_at`.
8. DDL correctness — CHECK constraints vs. the Pydantic models: No issues found. `n_parsed_sections >= 1`, `source_binary_hash` bounds, `sort_order >= 1`, `word_count >= 0`, `page_start`/`page_end >= 1`, the page ordering guard, and the both-null heading/content guard mirror `cleaned_models.Document` and `cleaned_models.Section`; the one model rule not expressible as a single-row CHECK (`sort_order` contiguity) is called out on lines 37-39.
9. Placeholder/substitution soundness: No issues found. The only placeholders are `{schema_name}`, `{document_table}`, and `{content_table}` — exactly the three `_utils.ensure_schema` substitutes — and no literal brace elsewhere in the file could trip its leftover-placeholder guard.
