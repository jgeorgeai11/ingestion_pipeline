---
name: cr-quality_report
goal: Address code quality issues identified in code/file_ingestion/quality_report.py to align with python-development skills.
created: 2026-06-23 12:57:35
updated: 2026-06-23 12:57:35
---

## Summary

`quality_report.py` is a well-built, genuinely generic, report-not-gate tool. The six quality checks are correct on inspection: `_is_content_bearing` faithfully mirrors the cleaner's keep rules (verified against `docling_section_parser.parse_docling_json`); the page classification holds its stated `has-body ⟺ real-content` invariant; blank-table / image-only / furniture-only / zero-elements tagging is right; the non-paginated `coverage=None` path is correctly excluded from the Tukey coverage fence so it neither crashes nor skews; the number-normalization in `_normalize_tokens` is correct (the `fullmatch` guard genuinely protects integers); the single-ALPHABETIC-char-word metric and markdown-table stripping are sound; and `analyze_document` isolates every per-document failure as an error entry without crashing or touching the exit code. No DB access, no SQL, and the optional section-number check is truly opt-in. Findings are minor: a check-numbering inconsistency in the docstrings, a non-paginated handling inconsistency between page-coverage and the fragmentation metric, and two defensible-but-worth-recording skill deviations (`print()` for the stdout deliverable and broad `except Exception` at the report boundary).

## Implementation Plan

1. [completed] Docstring / check-numbering consistency - `code/file_ingestion/quality_report.py`
   - RESOLVED: Renumbered fragmentation 6->5 and text-health 7->6 across the module docstring (lines 29, 31), the threshold comment (line 78), the per-function docstrings (`check_text_health` line 588, `compute_fragmentation_metrics` line 639, `flag_fragmentation` line 715, `analyze_document` line 829, `build_report` line 1041), and the `checkN` flag prefixes in `_collect_flags` (now `check5` line 949, `check6` line 955) so the six checks are contiguously 1-6; verified by re-running pub_100_01 which now renders `check5 fragmentation` and `check6 text-health` with no Check 5 gap and no stray check6-fragmentation/check7.
   - 1.1. [completed] Lines 16-32 and 29, 31, 545, 587, 636, 712: The module docstring numbers the checks 1, 2, 3, 4, **6**, 7 — there is no Check 5, yet the header (`six checks` in the task framing) and the actual code implement exactly six checks. `compute_fragmentation_metrics`/`flag_fragmentation` are labeled "Check 6" and `check_text_health`/`_section_text_health` are labeled "Check 7". A Check 5 was almost certainly removed or merged and the renumber never happened. Renumber fragmentation to Check 5 and text-health to Check 6 (or document why the gap exists), so the docstrings, the `checkN` flag prefixes in `_collect_flags` (lines 903, 911, 919, 925, 934, 942, 949, 955 use `check1`, `check2`, `check3`, `check4`, `check6`, `check7`), and the console output are internally consistent.
        - Current: docstring lists `4.`, then `6. Fragmentation extremes`, then `7. Text-health`; flag strings emit `check6` and `check7`.
        - Expected: contiguous numbering `1..6` across the module docstring, the per-function docstrings, and the `checkN` flag prefixes.

2. [completed] Non-paginated handling consistency between checks - `code/file_ingestion/quality_report.py`
   - RESOLVED: `flag_fragmentation` now excludes non-paginated docs (`total_pages == 0`) from both the `sections_per_page` Tukey distribution and the over/under-fragmentation `sections_per_page` legs (`high_spp`/`low_spp` are now gated on `paginated`), mirroring the `coverage is None` exclusion in `_coverage_threshold`; `median_words_per_section` (page-independent) still applies to every doc. Left `compute_fragmentation_metrics` unchanged (filter-only in the flagger is lower-risk than nulling the metric and avoids `round(None)`/comparison guards). Verified: pub_100_20 (a non-paginated source) now reports 0 documents flagged (no fragmentation false-flag) and still shows page coverage n/a; added regression test `TestFragmentation::test_non_paginated_doc_not_flagged_on_sections_per_page` with a mixed corpus where the old `pages=1` fallback would have flagged the Word doc.
   - 2.1. [completed] Lines 649-652: `compute_fragmentation_metrics` does not degrade non-paginated sources the way `check_page_coverage` does. `check_page_coverage` returns `coverage=None` and tags the doc `non-paginated` for `total_pages == 0`, but `compute_fragmentation_metrics` maps `total_pages=0 -> pages=1`, so `sections_per_page = section_count / 1 = section_count`. In a mixed corpus (a Word/DOCX doc alongside paginated PDFs) a large non-paginated doc then presents an inflated `sections_per_page` and can become a false over-fragmentation outlier in the shared Tukey fence built by `flag_fragmentation` (lines 733-735). Consider excluding non-paginated docs from the `sections_per_page` distribution/flagging (parallel to how `_coverage_threshold` at lines 1024-1028 excludes `coverage is None`), or skipping the `sections_per_page` reason for non-paginated docs. `median_words_per_section` is page-independent and is unaffected.
        - Current: `pages = total_pages if total_pages > 0 else 1` then `sections_per_page = section_count / pages`.
        - Expected: non-paginated docs excluded from the `sections_per_page` Tukey distribution / over-fragmentation flag (mirroring the coverage-fence exclusion), so a non-paginated source is not flagged as over-fragmented on a page count it does not have.

3. [completed] Exception-handling skill deviations (defensible, record with justification) - `code/file_ingestion/quality_report.py`
   - RESOLVED: Narrowed the parsed-JSON load catch (line 853) from broad `except Exception` to `except (OSError, ValueError)` — verified empirically that `DoclingDocument.load_from_json` raises `pydantic_core.ValidationError` (a `ValueError` subclass, confirmed via `issubclass`) on malformed/invalid-schema JSON and `OSError` on a read failure, so this is now consistent with the cleaned-JSON catch on line 858 (added a comment recording that). The checks-boundary catch (line 874) is kept broad by design (cr option a) with an added inline comment justifying it as the deliberate per-document isolation boundary: a malformed cleaned dict can raise KeyError/TypeError/AttributeError from any check and the report must never crash or change the exit code; narrowing it would silently break the never-crash contract. Resolved-as-documented.
   - 3.1. [completed] Lines 853 and 874: Broad `except Exception` is used when loading the parsed Docling JSON and when running the checks, whereas line 858 uses the specific `(OSError, ValueError)`. The exception-handling skill says to catch specific exceptions. Here the broad catch is defensible — it is the documented report-must-never-crash boundary that converts any per-document failure into an `error` entry without changing the exit code — but it is inconsistent with line 858 and with the skill. Recommend either (a) keeping the broad catch but adding a brief inline comment that the broad catch is the deliberate per-document isolation boundary, or (b) narrowing where the failure modes are known (`DoclingDocument.load_from_json` can be narrowed to its documented exceptions). At minimum, note the line 853 vs 858 inconsistency.
        - Current: `except Exception as e:` (lines 853, 874) vs `except (OSError, ValueError) as e:` (line 858).
        - Expected: consistent specificity, or an inline comment justifying the broad boundary catch.

4. [completed] Logging skill deviation (intentional, record) - `code/file_ingestion/quality_report.py`
   - RESOLVED (intended, no change): `print()` for the stdout deliverable is correct as-is — `logconfig.setup_logging` installs only a `FileHandler` on the root logger, so `logger.info()` writes only to the JSONL file and `print()` is the sole terminal channel (the dual emit does not double-print). Left unchanged per the task's explicit instruction; recorded as the deliberate stdout deliverable.
   - 4.1. [intended] Line 1096: `print(line)` is used in `_render_console`. The logging skill says "never `print()`". This is correct here and verified: `logconfig.setup_logging` installs only a `FileHandler` on the root logger (no `StreamHandler`), so `logger.info()` writes only to the JSONL file; the console summary is an explicit stdout deliverable, so `print()` is the right channel and the dual emit does not double-print to the terminal. The docstring at lines 1086-1088 correctly states this. Record as an intentional, justified deviation rather than a defect.
        - Current: `print(line)` plus `logger.info(line)` in `emit`.
        - Expected: no change required; keep as the deliberate stdout deliverable.

## Skills with No Issues

1. Type Hints: No issues found — every function and nested helper (`_quantile`, `emit`) has parameter and return annotations; modern syntax is used throughout (`dict[str, Any]`, `str | None`, `tuple[int, dict[int, str]]`); no stray `Optional`/`List`.
2. Docstrings: No issues found on coverage/style — all public and private functions carry Google-style docstrings with Args/Returns and document the "why" (the load-bearing `fullmatch` guard, the has-body invariant, the threshold tuning rationale). The only docstring issue is the check-numbering inconsistency captured in finding 1.1.
3. Comments: No issues found — comments explain "why" (e.g. lines 117-119 on the integer-corruption guard, lines 425-427 on the has-body invariant, lines 556-557 on excluding `\t\n\r` from control-char counting); none merely restate the code; no outdated comments observed.
4. Logging: One intentional deviation (finding 4.1) — `print()` is correct for the stdout deliverable given logconfig is file-only. Otherwise compliant: f-strings in log calls, run-boundary separators (lines 1164, 1222), context included in messages, no Entering/Exiting noise.
5. Exception Handling: Deviation recorded in finding 3.1 — broad `except Exception` at the per-document isolation boundary is defensible but inconsistent with the specific catch on line 858. No bare `except:`, exceptions carry context in the error-entry messages, and the report-not-gate contract is honored (a single bad document never raises or changes the exit code).
6. Executable Scripts: No issues found — `main()` with `if __name__ == "__main__"`, `--config` argparse argument, logging deferred until after argparse (lines 1160-1163 so `--help` creates no log files), config validation with `sys.exit(1)` only on usage/config error. The extra `--section-number-check` flag is an opt-in toggle, not a second config path, so the single-`--config` convention holds.
7. Data Validation: N/A — this is itself a generic quality/validation report tool (a reporter, not a `data_val_` script gating another module's output); it correctly does not gate (exit 0 except on its own usage/config error).
8. Unit Tests: N/A for this file — no tests are in scope for this review. Note for follow-up: the pure helpers (`_normalize_tokens`, `_is_content_bearing`, `_classify_pages`, `_tukey_fences`, `_median`, `_strip_markdown_tables`, `_section_text_health`, `check_section_numbers`) are highly testable and would benefit from a `unit_tests/test_quality_report.py` covering the number-normalization integer guard, the has-body invariant, the non-paginated coverage=None path, and the Tukey single-value collapse.
9. SQL (sql-development): N/A — the tool performs no database access and interpolates nothing into SQL; it reads only Docling JSON and cleaned-sections JSON from config-resolved directories.

## Status & Next Steps

**Current Status**: Review complete. The file is correct and generic on all six checks; findings are minor/consistency-level with no critical or major defects.
**Completed**:
1. Read and applied the code-review, python-development, and sql-development skills in full.
2. Reviewed all six checks for correctness, including `_is_content_bearing`, page classification and the has-body invariant, the Tukey two-pass, number-normalization, the single-alpha-char metric, and markdown-table stripping.
3. Verified `_is_content_bearing` and the keep-rule mirroring against `docling_section_parser.parse_docling_json` (lines 264-318) — they match (furniture/drop dropped, table kept on non-blank cell or caption, picture kept on caption, heading/TextItem kept on non-empty text).
4. Verified the config shape (`[parse].parsed_dir`, `[clean].cleaned_dir`, `[module].documents[].file`) against the repo configs and confirmed all current configs carry `documents`.
5. Verified the stdout-vs-file-handler claim by reading `logconfig.py`: only a `FileHandler` is installed on the root logger, so `print()` is the sole terminal channel and the dual emit does not double-print.
6. Confirmed exit-code discipline (exit 0 normally; `sys.exit(1)` only on usage/config error) and that `analyze_document` converts missing/unreadable parsed or cleaned JSON into error entries without crashing.
**Next Steps**:
1. Renumber the checks contiguously (finding 1.1).
2. Decide on non-paginated handling for the fragmentation `sections_per_page` metric (finding 2.1).
3. Optionally add an inline justification comment on the broad boundary catch (finding 3.1).
**Blockers**:
1. None.
**Notes**:
1. Strengths worth keeping: the load-bearing `fullmatch` integer-protection guard in `_normalize_tokens`; the `has-body ⟺ real-content` invariant enforced in `_classify_pages`; reusing `FURNITURE_LABELS`/`HEADING_LABELS`/`DROP_LABELS` from the cleaner so kept/dropped rules cannot drift; correctly excluding `coverage is None` docs from the Tukey coverage fence; and the genuinely opt-in, clearly content-specific `check_section_numbers`.
2. Per-document failure isolation, report-not-gate exit discipline, and no SQL/DB access all verified as required.
