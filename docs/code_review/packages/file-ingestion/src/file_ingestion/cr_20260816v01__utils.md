---
name: cr_20260816v01__utils
goal: Address code quality issues identified in code/file_ingestion/_utils.py to align with python-development skills, re-reviewed after the code/lib validator extraction and the COMMENT ON override feature.
created: 2026-08-16 19:29:01
updated: 2026-08-17 09:44:03
---

## Implementation Plan

1. [completed] Cross-module drift in the shared-library bootstrap and comment-override block - `code/file_ingestion/_utils.py`
   - 1.1. [minor] Lines 15, 19: The two imports that must follow `sys.path.insert` are not marked with `# noqa: E402`, while the sibling module `code/excel_ingestion/_utils.py` marks the byte-identical pattern (its lines 28 and 32). The same deliberate lint exception is handled two different ways across the two modules, so a lint run reports this file and not its sibling.
        - Current: `from logconfig import get_logger` / `from validators import validate_collection_path, validate_sql_identifier`
        - Expected: `from logconfig import get_logger  # noqa: E402` / `from validators import validate_collection_path, validate_sql_identifier  # noqa: E402`
        - Resolution: Implemented as specified — both post-`sys.path.insert` imports now carry `# noqa: E402`, matching the sibling `code/excel_ingestion/_utils.py` byte for byte. The new `sql_comments` import added by 1.2 sits in the same block and carries the marker too.
   - 1.2. [minor] Lines 111-139: The comment-override construction and execution loop is duplicated verbatim in `code/excel_ingestion/ingest_excel.py` `ensure_consolidated_tables` (lines 338-365) — same explanatory comment, same `list[tuple[str, str]]` accumulator, same `conn.execute(text(statement), {"comment_text": comment_text})` loop. The copies have already diverged: this one wraps `engine.begin()` in `try/except SQLAlchemyError` with an error log and re-raise, the excel copy has no error handling at all. This is the same failure mode as the previously-resolved validator duplication — promote the shared piece to `code/lib` (e.g. `sql_comments.build_comment_statements(...)`) and import it from both.
        - Current: `comment_statements: list[tuple[str, str]] = []` ... built inline in each module
        - Expected: a shared helper in `code/lib/` returning `list[tuple[str, str]]`, imported by `_utils.ensure_schema` and `ingest_excel.ensure_consolidated_tables`
        - Resolution: Implemented as specified, with two accommodations. Added `code/lib/sql_comments.py` with `build_comment_statements(db_schema, *, schema_comment=None, table_comments=None) -> list[tuple[str, str]]`; both `_utils.ensure_schema` and `ingest_excel.ensure_consolidated_tables` now call it, passing their two table names as an ordered `table_comments` dict (`{document_table: ..., content_table: ...}` / `{sheet_table: ..., content_table: ...}`), which is what lets one helper serve both legs' differently-named tables while keeping the schema-then-tables statement order the existing tests assert. Accommodation 1: the module also exports `COMMENT_TEXT_PARAM = "comment_text"` and both call sites bind with it, so the statement's placeholder and the caller's bind key can no longer drift apart. Accommodation 2: the helper re-validates the schema and every table-name key with `validators.validate_sql_identifier`, because it is now the place where identifiers are interpolated into SQL text (the callers still validate earlier, so no behavior changes). Transaction scope and error handling stay with each caller — the divergence noted in the finding (file leg wraps `engine.begin()` in `try/except SQLAlchemyError`, excel leg does not) is untouched here, since only the duplicated construction was in scope. Also added `code/lib/unit_tests/test_sql_comments.py` (12 tests, 100% coverage of the new module) covering ordering, None skipping, verbatim non-ASCII/quote text, the exported param name, and identifier rejection. Verified: `uv run pytest code` → 556 passed, and the excel leg's live-database comment tests (`test_pipeline_comment_keys_reach_the_catalog`, `test_pipeline_template_bakes_generic_comments_without_overrides`) pass against real PostgreSQL after the refactor.

2. [completed] Contracts asserted in code but not pinned elsewhere - `code/file_ingestion/_utils.py`
   - 2.1. [minor] Lines 85-87: The explicit `encoding="utf-8"` on `read_text` is load-bearing — `sql/schema.sql` line 68 carries an em dash, and the platform default (cp1252 on Windows) would silently mojibake it rather than raise. No test pins this: `unit_tests/test_utils.py::TestEnsureSchemaComments::test_real_template_renders_table_comments` asserts only ASCII substrings, so deleting the `encoding` argument leaves the whole suite green. See `cr_20260816v01_test_utils.md` (2.3) and `cr_20260816v01_schema.md` (4.1) for the paired findings.
        - Current: no assertion anywhere that non-ASCII template text survives the read
        - Expected: an assertion on the rendered template that the em dash from `sql/schema.sql` is intact (added in `test_utils.py`)
        - Resolution: Implemented as specified — `test_ensure_schema_real_template_renders_table_comments` now asserts `"—" in executed` with a comment stating that this is what pins the explicit `encoding="utf-8"` read. Verified the guard bites on this host: `locale.getpreferredencoding(False)` is `cp1252` and a platform-default `read_text()` of `sql/schema.sql` does not preserve the em dash, so removing the `encoding` argument now fails the assertion instead of passing silently. Paired findings implemented in the same changeset (`cr_20260816v01_test_utils.md` 2.3, `cr_20260816v01_schema.md` 4.1); the em dash was kept rather than replaced with an ASCII hyphen, and a live-database render confirmed the stored `document_content` description still contains it.
   - 2.2. [suggestion] Lines 33, 116-119: `ensure_schema` accepts a `schema_comment` override, but the shipped template `sql/schema.sql` sets no baseline `comment on schema`, unlike the two tables which do get generic template comments. A corpus whose config omits `[load].schema_comment` therefore leaves the schema with a NULL description, so the parameter's documented "None leaves the existing schema comment alone" resolves to "no description at all" in practice — asymmetric with the table parameters, whose None case still yields the template's generic text. See `cr_20260816v01_schema.md` (2.1).
        - Current: `schema_comment: str | None = None` with no template fallback
        - Expected: add a generic `comment on schema {schema_name} is '...'` to `sql/schema.sql` so the None case behaves like the table overrides
        - Resolution: Deferred — rejected 2026-08-17 (re-tagged minor → suggestion; the current form is deliberate and must not change): a baked-in `comment on schema` would run for EVERY config, including `config/test/ingest_test_document.toml`, which loads into the live `cms_iom` schema and deliberately carries no comment keys so it cannot touch production descriptions (activity `20260816v01_relocate_db_descriptions_and_grants`, decision 7); the template comment would overwrite the cms_iom schema description with generic text on every test run. The asymmetry is intentional: schema descriptions are per-corpus flavor and live only in config. The same design on the excel leg is test-pinned (`test_ingest_excel.py::test_pipeline_template_bakes_generic_comments_without_overrides` asserts the schema comment stays NULL without an override); `sql/schema.sql` follows the identical convention. Paired rejection: `cr_20260816v01_schema.md` 2.1.

3. [completed] Optional hardening - `code/file_ingestion/_utils.py`
   - 3.1. [suggestion] Line 104: The drift guard `re.findall(r"\{[^}]*\}", rendered_sql)` treats every brace pair as an unsubstituted placeholder. Legitimate DDL can contain braces — a regex quantifier in a CHECK (`~ '^[a-z]{2,4}$'`), a Postgres array literal (`'{a,b}'`), or a JSON default — and any of those would make `ensure_schema` raise on a valid template.
        - Current: `leftover_placeholders = re.findall(r"\{[^}]*\}", rendered_sql)`
        - Expected: `re.findall(r"\{(?:schema_name|document_table|content_table|[a-z_]+)\}", rendered_sql)` or an explicit known-placeholder allowlist
        - Resolution: Deferred — the shipped template contains no braces other than the three known placeholders, so the guard cannot misfire today, and its current broad form catches typo'd placeholder names that a narrow pattern would miss. Revisit only if the DDL gains a literal brace.
   - 3.2. [suggestion] Lines 112-114, 139: Binding `:comment_text` into a `COMMENT ON` statement works only because psycopg2 interpolates parameters client-side; PostgreSQL rejects server-side bind parameters in `COMMENT ON`. The dependency is documented in the comment but not enforced anywhere, so swapping the driver (psycopg3 defaults to server-side binding) would break every comment override at runtime with no compile-time signal.
        - Current: comment-only documentation of the psycopg2 coupling
        - Expected: note the driver requirement in the `ensure_schema` docstring as well, or assert `engine.dialect.driver == "psycopg2"` before issuing comment statements
        - Resolution: Deferred — `psycopg2-binary` is a pinned direct dependency in `pyproject.toml` and the coupling is already explained at the point of use; adding a runtime driver assertion would fail the mocked-engine tests for no current benefit.
   - 3.3. [suggestion] Lines 80-87: `ddl_path.exists()` followed by `read_text()` is a check-then-use race and a second path to `FileNotFoundError`, which `read_text()` would raise on its own.
        - Current: `if not ddl_path.exists(): ... raise FileNotFoundError(...)` then `template_sql = ddl_path.read_text(...)`
        - Expected: catch `FileNotFoundError` from `read_text` and re-raise with the same message, dropping the pre-check
        - Resolution: Deferred — the pre-check exists to emit the explicit "Schema SQL template not found" log line and error text that callers and tests match on; it was deliberately not applied in the 2026-06-22 review for the same reason.
   - 3.4. [suggestion] Lines 30-31: `document_table` and `content_table` are positional-with-defaults, and callers pass them positionally (`ingest.py` line 797). Transposing the two arguments is silent — both are valid identifiers — and produces a schema with the tables' roles swapped.
        - Current: `document_table: str = "document", content_table: str = "document_content", *,`
        - Expected: move both behind the existing `*` so they must be named at the call site
        - Resolution: Deferred — a signature change requiring a coordinated edit of every caller and test; the two names are adjacent and explicit at the single production call site, so the transposition risk is low.
   - 3.5. [suggestion] Line 1: The module docstring is a single line and does not state the re-export contract that lines 17-19 depend on, while the sibling `code/excel_ingestion/_utils.py` docstring enumerates its provided/re-exported names.
        - Current: `"""Shared utilities for file ingestion scripts."""`
        - Expected: a short `Provides:` block naming `ensure_schema` plus the two validators re-exported from `code/lib/validators`
        - Resolution: Deferred — the same information already appears in the inline comment on lines 17-18 and in `__all__`; duplicating it in the docstring is cosmetic.

## Skills with No Issues

1. Type Hints: No issues found — every parameter and return is annotated with modern syntax (`Engine`, `Path`, `str | None`, `-> None`), and the local `comment_statements: list[tuple[str, str]]` is annotated too.
2. Docstrings: No issues found — Google-style with Args/Raises, and `Raises:` is complete against the body (`FileNotFoundError`, `ValueError` for both the identifier and leftover-placeholder cases, `OSError`, `SQLAlchemyError`).
3. Comments: No issues found — the comments on lines 11-13, 17-18, 85-86, 101-103, and 111-114 all explain why (path resolution independent of cwd, re-export rationale, encoding rationale, drift guard, driver binding) rather than what.
4. Logging: No issues found — uses `logconfig.get_logger`, f-strings throughout, DEBUG for entry/intermediate detail and INFO for the single completion milestone, context values (schema, path, char count, override count) included, no `print()`.
5. Exception Handling: No issues found — specific exception types (`OSError`, `SQLAlchemyError`), `try/except/else` with logging in each branch, bare `raise` preserving the original type, and fail-fast validation before any I/O or DB call.
6. Executable Scripts: N/A — this is an imported library module with no `main()` or CLI surface.
7. Data Validation: N/A — the module validates SQL identifiers as an injection guard; dataset-level quality checks live under `data_validation/`.
8. Unit Tests: N/A for this file — reviewed separately in `cr_20260816v01_test_utils.md`.
