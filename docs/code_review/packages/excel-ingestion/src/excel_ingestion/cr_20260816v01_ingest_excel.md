---
name: cr_20260816v01_ingest_excel
goal: Address code quality issues in code/excel_ingestion/ingest_excel.py (re-review since 20260626v01, covering the COMMENT ON config keys and reviewed as a group with sql/excel_schema.sql and unit_tests/test_ingest_excel.py) to align with python-development and sql-development skills.
created: 2026-08-16 19:33:40
updated: 2026-08-17 09:53:30
---

## Implementation Plan

1. [completed] Config-time type guards so misconfiguration stays on `main()`'s clean-abort path - `code/excel_ingestion/ingest_excel.py`
   - 1.1. [minor] Line 565: `overwrite` is read with `config.get("overwrite", _DEFAULT_OVERWRITE)` and is never type-checked, unlike the row bounds (188-195), the comment keys (129-136), and `min_column_overlap` (141-149) which all have explicit guards. A quoted TOML value (`overwrite = "false"`) parses to the string `"false"`, which is truthy, so `write_consolidated`'s `if present and not overwrite` (462) and `if present and overwrite` (468) take the OVERWRITE branch: the existing `sheet` row is deleted (cascading to `sheet_content` and the structured rows) and re-written, silently doing the opposite of what the config author asked. This is the only config key whose wrong type changes behaviour silently rather than raising.
        - Current: `overwrite = config.get("overwrite", _DEFAULT_OVERWRITE)` with no validation in `validate_config`
        - Expected: in `validate_config`, `if "overwrite" in config and not isinstance(config["overwrite"], bool): raise ValueError(f"overwrite must be a boolean, got {config['overwrite']!r}")`
        - Resolution: Implemented as specified — added the `overwrite` boolean guard in `validate_config` (with a comment recording that this is the one key whose wrong type flips the branch silently rather than raising). Covered by the new `test_validate_config_non_bool_overwrite_raises` / `test_validate_config_accepts_bool_overwrite`.
   - 1.2. [minor] Lines 109, 116-117, 242: `validate_sql_identifier` is the guard for `db_schema`, `sheet_table`, `content_table`, and the per-sheet `table`, but its implementation (`code/lib/validators.py:39`) calls `_SAFE_IDENTIFIER_RE.fullmatch(name)`, which raises `TypeError` — not `ValueError` — for a non-string value (verified: `validate_sql_identifier(5, "db_schema")` -> `TypeError: expected string or bytes-like object, got 'int'`). `main()` only catches `ValueError` around `validate_config` (551), so a TOML `db_schema = 5` escapes the clean `logger.error` + `sys.exit(1)` abort and surfaces as an unhandled traceback.
        - Current: `validate_sql_identifier(config["db_schema"], "db_schema")` (and the same for `sheet_table`/`content_table`/`table`) with no string check first
        - Expected: assert the type before delegating, e.g. `if not isinstance(config["db_schema"], str): raise ValueError(f"db_schema must be a string, got {config['db_schema']!r}")` (applied to all four identifier fields), so every config-shape error funnels through the `except ValueError` abort
        - Resolution: Implemented with a shared helper instead of four inline blocks — added the private `_require_str(value, label)` (raises `ValueError: {label} must be a string, got {value!r}`) and called it before `validate_sql_identifier` for `db_schema`, `sheet_table`, `content_table`, and the per-sheet `table` (the last labelled `files[{filename!r}].sheets[{j}]: table`, matching the surrounding per-sheet messages). Message text and behaviour are as specified; the helper avoids repeating the same three lines five times (it also serves 1.3). Covered by the new `test_validate_config_non_string_top_level_field_raises` (parametrized over `db_schema`/`sheet_table`/`content_table` as well as the 1.3 fields) and `test_validate_config_non_string_table_raises`.
   - 1.3. [minor] Lines 555-558 (read), 577 and 594 (use): `source_dir` and `db_name` are required-and-present-checked (105-107) but never type-checked. A non-string `source_dir` reaches `Path(source_dir)` at line 594 and raises `TypeError` (verified: `Path(5)` -> `TypeError: argument should be a str or an os.PathLike object ...`) outside any handler, and a non-string `db_name` reaches `get_engine` inside the `(ValueError, FileNotFoundError, SQLAlchemyError)` block (584) which does not catch `TypeError` either. Same escape as 1.2, for the two remaining required top-level fields.
        - Current: `source_dir = config["source_dir"]` / `db_name = config["db_name"]` with only a presence check
        - Expected: validate both as `str` in `validate_config` alongside the other top-level guards
        - Resolution: Implemented as specified — `_require_str(config["source_dir"], "source_dir")` and `_require_str(config["db_name"], "db_name")` run immediately after the required-field loop, so both are checked before any other work. The docstring's first paragraph and the new second paragraph record the string/boolean guards and why they belong at config time. All four shipped configs (`config/example.toml`, the three `config/qpp_cm/*.toml`) still pass `validate_config` unchanged.

2. [completed] `write_consolidated`'s non-empty `rows` precondition is undocumented - `code/excel_ingestion/ingest_excel.py`
   - 2.1. [minor] Lines 424 (signature), 446 (Args), 487 (`"n_rows": len(rows)`): the function inserts `n_rows = len(rows)` into the `sheet` table, whose DDL constrains `check (n_rows >= 1)` (`sql/excel_schema.sql:28-29`), and then executes the `sheet_content` insert with the per-row parameter list. Calling it with `rows=[]` therefore cannot succeed. `main()` guards this at 628-632 (the zero-row skip) with a comment explaining why, but the function's own docstring (430-454) documents neither the precondition nor a `Raises:` section, so a future caller has no contract to read. Every other public function in the module documents its `Raises` (`validate_config` 99-104, `ensure_consolidated_tables` 308-312).
        - Current: `rows: Parsed row dicts.` in the Args block, no `Raises:` section
        - Expected: state the precondition (`rows` must be non-empty — the `sheet` row's `n_rows >= 1` CHECK rejects an empty sheet) and add a `Raises: SQLAlchemyError` section, matching the module's other public functions
        - Resolution: Implemented as specified — added a "Precondition: `rows` must be non-empty" paragraph to the docstring (naming the `n_rows = len(rows)` insert, the `check (n_rows >= 1)` DDL in `sql/excel_schema.sql`, and the caller's obligation to skip a zero-row sheet as `main()` does), annotated the `rows` Arg accordingly, and added a `Raises: SQLAlchemyError` section noting the whole-sheet rollback and the empty-`rows` CHECK violation.

3. [completed] `build_row_text` collapses falsy values to empty - `code/excel_ingestion/ingest_excel.py`
   - 3.1. [minor] Line 389: `row.get(col) or ''` renders any falsy value as the empty string, not just `None`. The signature accepts `dict[str, str | int | None]` (373), so an `int` `0` or an empty-string value would silently render as `Header: ` and be baked into both `row_text` and the `source_binary_hash` fingerprint. It cannot bite today because `excel_parser._cell_value` (`excel_parser.py:411-415`) coerces every cell to a stripped string and maps empty to `None` (so `0` arrives as the truthy `"0"`), and `ROW_NUMBER_KEY` — the only `int` in a row dict — is never in `column_names`. Promoted from suggestion 2026-08-17 (re-rated to minor): the deferral reason was present-day unreachability only; land together with the paired test `cr_20260816v01_test_ingest_excel.md` 5.4.
        - Current: `parts = [f"{col}: {row.get(col) or ''}" for col in column_names]`
        - Expected: `parts = [f"{col}: {'' if row.get(col) is None else row.get(col)}" for col in column_names]`
        - Resolution: Implemented as specified (comprehension wrapped over two lines for the line-length limit), plus a comment on why the test is `is None` rather than truthiness (an `int` 0 would otherwise be lost from both `row_text` and the `source_binary_hash` computed over it) and a docstring change from "None values render as empty" to "Only `None` ... renders as empty; a falsy-but-present value such as `0` or `\"\"` renders verbatim". Landed with the paired test `cr_20260816v01_test_ingest_excel.md` 5.4 (`test_build_row_text_falsy_but_present_value_survives`, parametrized over `0`, `""`, `"0"`).

4. [completed] SQL identifier quoting and template-guard consistency - `code/excel_ingestion/ingest_excel.py`
   - 4.1. [suggestion] Lines 409, 471, 479, 503: the consolidated-leg SQL interpolates `{db_schema}.{sheet_table}` / `{content_table}` unquoted, while `structured_table.py` double-quotes every generated identifier via `_quote` (`structured_table.py:49-61`). Both are injection-safe (all three names pass `validate_sql_identifier`'s `fullmatch` of `[a-z_][a-z0-9_]*` before any interpolation, at 109/116-117 and again at 313-315), and the lowercase-only pattern means quoting cannot change resolution either. Carried forward from the 2026-06-26 review (item 2.1); the code is unchanged.
        - Current: `f"select 1 from {db_schema}.{sheet_table} where collection_path = :cp"`
        - Expected: (optional) adopt `structured_table._quote` here for one quoting convention across the module
        - Resolution: Deferred — cosmetic consistency only; the validator restricts identifiers to lowercase `[a-z_][a-z0-9_]*`, so the unquoted and quoted forms are equivalent and neither is injectable.
   - 4.2. [suggestion] Line 331: `re.findall(r"\{[^}]*\}", rendered)` flags ANY surviving brace pair, so it would false-positive if the DDL ever gained a literal brace (an array default `'{}'`, a `$$ ... {} ... $$` body). The current template (`sql/excel_schema.sql`) contains no literal braces, so the guard catches exactly its intended case — a placeholder the code forgot to substitute. Carried forward from the 2026-06-26 review (item 3.1).
        - Current: `leftover = re.findall(r"\{[^}]*\}", rendered)`
        - Expected: `leftover = re.findall(r"\{(?:schema_name|sheet_table|content_table)\}", rendered)`
        - Resolution: Deferred — latent only; the template has no literal braces today, and the broader regex is the safer default until one is introduced.
   - 4.3. [suggestion] Lines 114-124 vs 240-242: `validate_config` now rejects `sheet_table == content_table` (the 2026-06-26 [minor] is RESOLVED), but a per-sheet `table` equal to `sheet_table` or `content_table` is still accepted. Such a config sends the structured leg at a consolidated table: `structured_table.reconcile_columns` reflects the consolidated columns (`title`/`n_rows`/... or `row_text`/`word_count`), finds zero overlap with the incoming `col_*` names, and raises its "Likely a wrong-table-name clash" `ValueError`, which `main()` records as a per-sheet structured failure (698-710) and exits 1 — so the misconfiguration is caught, just per-sheet and with a column-overlap message rather than a name-collision one.
        - Current: only `sheet_table != content_table` is enforced
        - Expected: also reject any per-sheet `table` equal to the resolved `sheet_table` or `content_table`
        - Resolution: Deferred — the overlap guard already fails these sheets loudly with a `ValueError` and a non-zero exit; the added check only improves the error message.

5. [completed] Orchestration and reporting (5.2 promoted; others deferred) - `code/excel_ingestion/ingest_excel.py`
   - 5.1. [suggestion] Lines 593-710: the per-sheet body of `main()` (parse -> zero-row skip -> collection_path -> title -> embed -> structured, each with its own failure record) is ~120 lines inside a doubly-nested loop, so `main()` mixes orchestration with per-sheet business logic; the executable-scripts pattern keeps `main()` as argparse + config + a call into the logic functions.
        - Current: the whole per-sheet pipeline is inline in `main()`
        - Expected: extract `_ingest_sheet(engine, config_values, filename, sheet_entry) -> dict | None` returning a failure record (or None) and let `main()` loop over the results
        - Resolution: Deferred — structural refactor with no behavioural change; the inline form keeps the four failure stages and their `continue` semantics visible in one place, and the stage comments already delimit them.
   - 5.2. [minor] Lines 588-590 and 712-715: skipped sheets are invisible in the summary. A sheet skipped by the overwrite=false presence check returns the `-1` marker (467) and is excluded from `embedded`, and a zero-row sheet is skipped at 628-632, but both still count in `total_sheets` — so a fully-successful idempotent re-run logs `Summary: sheets embedded 0/22 ... 0 failure(s)`, which reads like a failure. Promoted from suggestion 2026-08-17 (re-rated to minor): activity `20260816v01_relocate_db_descriptions_and_grants` made idempotent re-runs the standard comment-refresh mechanism (its task 8.5 sweep re-runs every config with `overwrite = false`), so this summary is now the normal output of a successful run — reporting correctness for the primary workflow, not polish.
        - Current: `f"Summary: sheets embedded {embedded}/{total_sheets}, structured {structured}; {len(failures)} failure(s)"`
        - Expected: track a `skipped` counter (incremented on the `-1` marker and on the zero-row skip) and report `embedded X, skipped Y, structured Z of N sheet(s)`
        - Resolution: Implemented as specified — added a `skipped` counter initialised next to `embedded`/`structured`, incremented in the zero-row skip and in a new `else` on the `marker >= 0` check (with a comment on why the `-1` marker is counted), and the summary now reads `Summary: embedded {embedded}, skipped {skipped}, structured {structured} of {total_sheets} sheet(s); {n} failure(s)`. Note: the structured leg's own `-1` skip is still not counted anywhere — `structured` remains "structured writes performed", per the documented fix. The superseded wording in the completed activity `docs/activities/excel-ingestion/consolidate-excel-ingestion.md` (task 8.4) was left as the historical record.
   - 5.3. [suggestion] Lines 35-44 vs 50: the imports that follow the first `sys.path.insert` (34) — `_utils`, `excel_parser`, `structured_table` — carry no `# noqa: E402`, while the `logconfig` import after the second insert (49) does. Both blocks are module-level imports after executable code, so the annotation is inconsistent within one file (the same split exists in `excel_parser.py:36` vs `:42`).
        - Current: `from _utils import (...)` / `from excel_parser import ...` / `import structured_table` with no marker
        - Expected: add `# noqa: E402` to the first block too (or drop it from line 50) so the file reads consistently
        - Resolution: Deferred — no linter is configured in `pyproject.toml`, so neither form is enforced; purely cosmetic.

## Skills with No Issues

1. Type Hints: No issues found — every function annotates parameters and returns with modern syntax (`str | None`, `list[dict[str, str | int | None]]`, `int`, `main() -> None`), and the keyword-only comment parameters on `ensure_consolidated_tables` (277-279) are `str | None`. The bare `dict` on `validate_config` (73) is appropriate for a `tomllib.load` result whose shape is exactly what the function validates.
2. Docstrings: Google-style with Args/Returns/Raises throughout and current with the new behaviour — `validate_config` (74-104) documents the comment keys, the `sheet_table`/`content_table` identifier checks, and the table_comment-requires-table rule; `ensure_consolidated_tables` (281-312) documents the override-wins-over-template ordering, the unconditional re-application, and the parameter binding. The one gap is finding 2.1 (`write_consolidated`'s precondition/Raises).
3. Comments: No issues found — comments explain "why": why equal consolidated names are rejected (118-120), why comment text is validated even though it is bound as data (126-128), why `min_column_overlap` is range-checked here rather than in `write_rows` (138-140), why UTF-8 is forced on the DDL read (320-321), why COMMENT ON with a bind parameter is safe under psycopg2 (338-341), why the embed failure suppresses the structured leg (682-683), and why zero-row sheets are skipped (625-627).
4. Logging: No issues found — `setup_module_logging` deferred until after argparse (531 -> 534), `"=" * 60` separators at 535 and 721, f-strings everywhere, no `print`; the new comment feature is reported in the ensured-tables line (`{len(comment_statements)} comment override(s) applied`, 366-370) and the config line reports the resolved table names and flags (570-574).
5. Exception Handling: No issues found for the paths that are caught — specific types at every stage (`(tomllib.TOMLDecodeError, OSError)` 544, `ValueError` 551, `(ValueError, FileNotFoundError, SQLAlchemyError)` 584, `(FileNotFoundError, ValueError)` 613, `SQLAlchemyError` 672, `(ValueError, SQLAlchemyError)` 698), no bare `except`, each handler logs with context and records a per-sheet failure, and config-level errors abort. Task 1 covers the wrong-typed-config values that never become `ValueError` in the first place.
6. Executable Scripts: No issues found — single required `--config` argument, `main()` + `if __name__ == "__main__"`, config-existence check before the read (537-539), TOML configs under `code/excel_ingestion/config/`.
7. Data Validation: N/A — this is the orchestrator; the `data_val_` scripts live in `code/excel_ingestion/data_validation/`.
8. Unit Tests: N/A — reviewed separately in `docs/code_review/excel_ingestion/unit_tests/cr_20260816v01_test_ingest_excel.md`, which records the matching coverage gap for the type guards in task 1.
9. SQL Best Practices: No issues found — all SQL is lowercase, every value is bound (`:cp`, `:title`, `:n_rows`, `:hash`, `:structured_table`, `:comment_text`, and the executemany content parameter list at 493-509), every insert lists its columns explicitly, and the column lists match the DDL in `sql/excel_schema.sql` (25-55) exactly.
