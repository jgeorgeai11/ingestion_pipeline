---
name: cr-test_ingest_excel
goal: Verify the new configurable sheet_table/content_table tests in code/excel_ingestion/unit_tests/test_ingest_excel.py are real (not tautological) and that the default-path e2e tests still pin sheet/sheet_content, aligning with the unit-tests skill. Conftest findings folded in.
created: 2026-06-26
updated: 2026-06-26
---

## Implementation Plan

1. [pending] Custom-table e2e: cascade claim is slightly overstated for the structured leg - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 1.1. [suggestion] Line 600 (`test_pipeline_custom_consolidated_table_names`): the docstring (line 661-662) claims the overwrite re-run exercises "the FK cascade (custom sheet delete cascades to custom content + structured rows)". The CONTENT cascade is genuinely proven — `write_consolidated` deletes only the `xls_sheet` row (`ingest_excel.py:383-390`) and relies on the FK to clear `xls_sheet_content`; without cascade the old `(wb.alpha, 1..2)` content rows would collide with the new `(wb.alpha, 1)` insert on the composite PK and the txn would fail, so `content_after == 1` (line 685) pins it. The STRUCTURED-row cascade is NOT independently proven here: `structured_table.write_rows(overwrite=True)` does its own `delete ... where collection_path` (`structured_table.py:301-311`), so `structured_after == 1` (line 686) would hold even if the FK cascade never fired.
        - Current: the test asserts `structured_after == 1` after an overwrite re-run and the docstring attributes it to the FK cascade.
        - Expected: either reword the docstring to claim only the content cascade for the custom name (the structured count is pinned by `write_rows`'s own delete, not the cascade), or add a direct-delete assertion against the custom sheet table (delete the `xls_sheet` row, assert the `alpha` structured rows vanish) to prove the cascade on the custom name as `test_deleting_parent_sheet_cascades_structured_rows` does for the default look-alike.
        - Rationale: unit-tests 7 — an assertion's stated guarantee should match what it actually discriminates; the FK `on delete cascade` is name-independent so the risk is low, hence [suggestion].

2. [pending] Untested validate_config type guards - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 2.1. [minor] The three `isinstance(..., dict)` config guards are uncovered (coverage marks `ingest_excel.py:127, 133, 149` missed): non-dict `files` (line 126-129, "must be a table of workbooks"), non-dict `file_entry` (line 132-136, "must be a table"), and non-dict `sheet_entry` (line 148-152, "must be a table"). `test_validate_config_sheets_not_a_list_raises` covers the sibling `sheets`-not-a-list guard, so these three are an asymmetric gap.
        - Current: no test passes a non-dict `files`, file entry, or sheet entry.
        - Expected: one parametrized test (or three) setting `config["files"] = []`, `config["files"]["wb.xlsx"] = "oops"`, and `config["files"]["wb.xlsx"]["sheets"][0] = "oops"`, each asserting `pytest.raises(ValueError, match="must be a table")`.
        - Rationale: unit-tests 7.1 — documented type-validation branches with no test; symmetric with the already-covered `sheets`-not-a-list case.

3. [pending] Untested main() resilience siblings - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 3.1. [suggestion] Two per-sheet failure stages remain uncovered (coverage marks `ingest_excel.py:550-563` and `582-594` missed): the collection_path resolution failure branch (a degenerate sheet name that fails `make_collection_path` -> recorded failure, exit 1) and the embed-leg `SQLAlchemyError` branch (which records the failure AND skips the structured leg via `continue`, line 594). The prior round added the parse and structured failure tests (`test_pipeline_resilient_parse_failure`, `test_pipeline_resilient_mixed_batch`); these two siblings of the same resilience contract are still open.
        - Current: only the parse-stage and structured-stage failure branches are exercised end-to-end.
        - Expected: a config with a sheet whose derived path is degenerate (e.g. a sheet named so its label sanitizes to empty) alongside a good sheet, asserting exit 1 + the good sheet still loaded (collection_path stage); the embed-`SQLAlchemyError` branch is harder to provoke without fault injection and may stay accepted.
        - Rationale: unit-tests 7.1 — the resilience contract ("one sheet's failure does not abort others") has four stages; two are now pinned, two are not.

4. [pending] ensure_consolidated_tables guard tests (previously-accepted gap) - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 4.1. [suggestion] The two `ensure_consolidated_tables` guards remain untested (coverage marks `ingest_excel.py:262, 275` missed): the missing-DDL `FileNotFoundError` (line 261-262) and the unsubstituted-placeholder `ValueError` (line 274-278). The configurable-table feature added two more placeholders (`{sheet_table}`, `{content_table}`) to the substitution at lines 264-269, so the leftover-placeholder guard now protects more substitutions — but the new e2e (`test_pipeline_custom_consolidated_table_names`) already exercises the happy-path substitution of all three placeholders end-to-end, so the guard itself is pure defense-in-depth against template/code drift.
        - Current: no test points the DDL path at a missing file or at a template containing a stray `{foo}`.
        - Expected: monkeypatch `Path(__file__).parent / "sql" / "excel_schema.sql"` to a missing path (assert `FileNotFoundError`) and to a template with a stray placeholder (assert `ValueError, match="Unsubstituted placeholder"`). Requires fragile internal monkeypatching; the 2026-06-24 review consciously skipped this as low-value drift-protection.
        - Rationale: unit-tests 7.1 — documented `Raises:` conditions with no test; kept at [suggestion] and not re-escalated despite the added placeholders, since the happy-path substitution is now covered.

## Skills with No Issues

1. unit-tests (the new custom-table tests are REAL, not tautological): No issues — verified against the source and empirically (all 115 tests ran with a live DB, no skips). `test_validate_config_accepts_custom_consolidated_table_names` (line 172) and the parametrized `test_validate_config_unsafe_consolidated_table_name_raises` (line 180, over `["sheet_table", "content_table"]`) exercise the real validators at `ingest_excel.py:105-110`; the `match="Unsafe SQL identifier for {table_key}"` strings are confirmed by the passing run. The e2e `test_pipeline_custom_consolidated_table_names` (line 600) proves all four required facts: (a) custom tables created + populated (`_table_exists` + `meta==1`/`content==2`, lines 633-653); (b) defaults NOT created (`not _table_exists(... "sheet")` / `"sheet_content"`, lines 656-657) — discriminating because the schema is a fresh UUID schema where only configured names get DDL; (c) the structured FK references the CUSTOM sheet table — `ensure_table` emits `references {schema}.{sheet_table}` (`structured_table.py:113-114`), so if the FK pointed at the absent default `sheet`, `create table alpha ... references ...sheet` would fail at creation time; `structured==2` succeeding proves the FK target resolved to `xls_sheet`; (d) the overwrite re-run exercises the `_content_source_present` select and the content cascade against the custom name (see finding 1.1 for the structured-cascade nuance).
2. unit-tests (default path still pins sheet/sheet_content): No issues — the default e2e helpers `_make_workbook`/`_write_config`/`_content_rows`/`_sheet_row` (lines 251-317) hard-code `sheet`/`sheet_content` in their read-back SQL, so `test_pipeline_embed_only_and_both_legs`, `test_pipeline_stores_title_n_rows_and_hash`, `test_pipeline_authored_collection_path_and_title`, `test_pipeline_skip_if_present_then_overwrite`, `test_pipeline_skips_zero_row_sheet`, `test_pipeline_resilient_mixed_batch`, and `test_pipeline_resilient_parse_failure` all assert against the default consolidated table names — the defaults remain covered when the custom-table path is added.
3. unit-tests (assertion strength — hash + row_text): No issues — `test_pipeline_stores_title_n_rows_and_hash` recomputes the expected fingerprint via `compute_source_hash([...])` and asserts equality + nonzero (line 391-395), discriminating a constant/0 implementation; `test_pipeline_embed_only_and_both_legs` pins the persisted `row_text == "Code: A1\nLabel: alpha"` and `word_count == 4` (lines 346-347) at the persistence boundary. The prior round's 1.1/2.1 findings are resolved.
4. unit-tests (skip != re-insert): No issues — `test_pipeline_skip_if_present_then_overwrite` captures `before` and asserts `after == before` (lines 442-449), proving the skip leaves the `sheet` metadata row byte-identical (the prior 4.1 finding is resolved).
5. unit-tests (pytest.raises match): No issues — empirically confirmed by the all-passing live-DB run. Config `match=` strings ("Missing required config field", "missing required", "Excel column letter", "at or after start_col", "Invalid collection_path", "must be greater than header_row", "must be >= data_start_row", "Unsafe SQL identifier", "Unsafe SQL identifier for sheet_table/content_table", "missing required field 'sheets'", "must be an integer", "must be a list", "min_column_overlap") all exist in `ingest_excel.py` / the delegated validators.
6. unit-tests (mock realism / no over-mocking): No issues — config tests are pure (no I/O); pipeline tests run real openpyxl workbooks through real `ingest_excel.main()` against the real `ephemeral_schema` DB, skipping cleanly without a DB. No DB or filesystem mocking that would make a test tautological. The custom-table test inlines its TOML (lines 617-627) rather than using `_write_config` precisely so the top-level custom names are set — a deliberate, correct choice.
7. unit-tests (parametrize / naming / AAA / order independence): No issues — `test_validate_config_unsafe_consolidated_table_name_raises` parametrizes the two table keys over one runner; names follow `test_<function>_<scenario>_<expected>`; each test arranges a fresh config dict / workbook (`_valid_config()` returns a fresh dict per call; `ephemeral_schema` yields a unique UUID schema per test); no shared state.
8. unit-tests (conftest fixture realism + teardown): No issues — `conftest.py`'s `ephemeral_schema` creates a UUID-named throwaway schema, `pytest.skip`s cleanly when the DB is unreachable, and drops the schema with CASCADE in a `finally` (so teardown runs on assert/raise) with `engine.dispose()` always called. The dead `mock_engine` fixture flagged in the 2026-06-24 round has been removed.
9. type-hints: No issues — all helpers (`_valid_config`, `_run`, `_write_config`, `_content_rows`, `_sheet_row`, `_make_workbook`, `_table_exists`) and tests are annotated.
10. docstrings: No issues — every test and helper has a behavior docstring.
11. logging: No issues — `setup_logging` configured to `logs/excel_ingestion/unit_tests`.
12. sql-development: N/A — SQL appears only in test read-back helpers as parameterized queries against the ephemeral schema; the `select *` in `_content_rows`/`_sheet_row` is test introspection, not a query on raw inputs under review.

## Status & Next Steps

**Current Status**: Reviewed (review-only, no edits). Suite green: 115 passed with a live DB (every test ran, no skips). The new configurable `sheet_table`/`content_table` tests are real and discriminating, not tautological — all four required facts (custom tables created+populated, defaults NOT created, structured FK references the custom sheet table, overwrite/content-cascade exercised) are genuinely proven. Default-path e2e tests still pin `sheet`/`sheet_content`. Findings are one [minor] (untested config type guards) and three [suggestion] (cascade-claim wording, two resilience siblings, the previously-accepted ensure_consolidated_tables guards).
**Completed**:
1. Verified the custom-table validate_config and e2e tests against `ingest_excel.py` (validators at 105-110, the threaded `sheet_table`/`content_table` at 478-479 / 493 / 600) and `structured_table.py` (FK at 113-114) and `excel_schema.sql` (FK at 45-47).
2. Confirmed every default-path e2e test reads back the literal `sheet`/`sheet_content` tables, so defaults stay covered.
3. Ran `pytest --cov` (then deleted `.coverage`): `ingest_excel.py` at 88%; the misses are the config type guards (127/133/149), `ensure_consolidated_tables` guards (262/275), main() CLI/setup exits (453-468/494-496), the collection_path + embed resilience branches (550-563/582-594), and the `__main__` guard (637).
**Next Steps**:
1. Reword or strengthen the custom-table cascade claim (1.1).
2. Add the three config type-guard tests (2.1) and the collection_path resolution-failure resilience test (3.1).
**Blockers**:
1. None.
**Notes**:
1. The cascade nuance (1.1) is low-risk: the FK `on delete cascade` is defined name-independently in the rendered DDL, and the dedicated `test_deleting_parent_sheet_cascades_structured_rows` covers structured cascade against the default look-alike.
2. main() CLI/setup exits (453-468, 494-496) and the `__main__` guard (637) are entry-point-only paths; covering them needs argv/env manipulation and remains lower value, consistent with prior rounds.
