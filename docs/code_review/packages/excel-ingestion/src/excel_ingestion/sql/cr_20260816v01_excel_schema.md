---
name: cr_20260816v01_excel_schema
goal: Address SQL quality issues in code/excel_ingestion/sql/excel_schema.sql (re-review since 20260624v01, reviewed as a group with ingest_excel.py and unit_tests/test_ingest_excel.py) to align with sql-development core skills.
created: 2026-08-16 19:33:40
updated: 2026-08-17 09:53:30
---

## Implementation Plan

1. [completed] Comment literals exceed the 100-character line limit - `code/excel_ingestion/sql/excel_schema.sql`
   - 1.1. [minor] Lines 63 and 66: the two `comment on table` description literals are single physical lines of 201 and 196 characters (measured), against the sql-development formatting rule of max 100 characters per line. Every other line in the file complies. SQL string constants can be split across physical lines — two literals separated by whitespace containing at least one newline are concatenated by the parser — so the text can be wrapped without changing the stored description. Note the coupling flagged in the group's test review: `test_ingest_excel.py:741` and `:743` assert the substrings `"Catalog of ingested Excel worksheets"` and `"row-level text"`, so any rewrap must keep those substrings inside a single literal fragment.
        - Current: `    'Catalog of ingested Excel worksheets — one row per sheet (collection_path) with title, n_rows, structured_table (the structured table it was loaded into, if any), source_binary_hash, ingested_at';` (one 201-char line)
        - Expected: wrap using adjacent string literals, e.g. `    'Catalog of ingested Excel worksheets — one row per sheet '` / `    '(collection_path) with title, n_rows, structured_table (the '` / `    'structured table it was loaded into, if any), source_binary_hash, ingested_at';`
        - Resolution: Implemented as specified, with the fragment split points chosen slightly differently from the illustrative Expected so each of the three fragments fits the limit and neither test-asserted substring is cut: the sheet description breaks after `... (collection_path) ` and after `... it was loaded `, the content description after `... one row per ` and after `... newline key-value `. Verified programmatically that the concatenated text is byte-identical to the previous single-line literals, that `"Catalog of ingested Excel worksheets"` and `"row-level text"` each sit inside one fragment (so `test_ingest_excel.py:741`/`:743` still pass — they do, in the full run), and that no line in the file now exceeds 100 characters. Added a comment above the block noting that the split literals are concatenated by the parser and that each fragment's trailing space is therefore significant.

2. [completed] Constraint and description coverage - `code/excel_ingestion/sql/excel_schema.sql`
   - 2.1. [suggestion] Line 27: `title text not null` has no non-empty CHECK, while the sibling `row_text text not null check (row_text <> '')` (50-51) does, and the file header (11-12) states the CHECKs "mirror the application-level invariants as defense in depth". A config-authored `title = ""` (`ingest_excel.py:658-660` only defaults the title when it is absent, it does not reject an empty one) would be stored as an empty human-readable title.
        - Current: `title text not null,`
        - Expected: `title text not null\n        check (title <> ''),`
        - Resolution: Deferred — mirrors `file_ingestion/sql/schema.sql:21`, which also leaves `title` unchecked; changing it here alone would break the deliberate symmetry between the two ingestion schemas, and the practical exposure is a hand-authored empty title in config. IMPLEMENTED 2026-08-21 by `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.1): the corpus rebuild made template DDL additions reachable, so `check (trim(title) <> '')` landed here TOGETHER with the same check (plus the strengthened not-both-empty section guard) in `file_ingestion/sql/schema.sql`, preserving the symmetry that was the reason for deferring each alone. Verified against live PostgreSQL by a real-database test (whitespace-only title rejected by the DB); zero stored sheet rows violate the check.
   - 2.2. [suggestion] Lines 25-55: the two tables carry `comment on table` descriptions but no `comment on column` entries, even though the header states the descriptions exist to be surfaced to an MCP `list_tables` consumer. Column-level descriptions (`col_description`) are what a text-to-SQL consumer needs to pick `collection_path` vs `sort_order` vs `row_text` correctly, and the table comment currently carries that information as prose inside one long string (the same string that triggers finding 1.1).
        - Current: table-level `comment on table` only
        - Expected: add `comment on column {schema_name}.{sheet_table}.collection_path is '...'` (and siblings) so each column is described in the catalog
        - Resolution: Deferred — optional enhancement; the table-level prose already names every column for the retrieval consumer, and `file_ingestion/sql/schema.sql` follows the same table-only convention, so adding column comments here alone would diverge from the sibling schema. Re-decided 2026-08-21 in `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.2): still deferred — `comment on` statements execute unconditionally on every ingest (unaffected by the `create table if not exists` migration caveat), so column comments can land whenever a column-level catalog consumer exists, with no rebuild needed.
   - 2.3. [suggestion] Lines 44-55: `{content_table}` has only its `(collection_path, sort_order)` primary key, yet its own description (66) advertises it for "keyword search across all workbook data" — such a search on `row_text` will sequential-scan.
        - Current: no index on `row_text`
        - Expected: (when the corpus grows) `create index if not exists {content_table}_row_text_trgm on {schema_name}.{content_table} using gin (row_text gin_trgm_ops);` plus the `pg_trgm` extension, or a `tsvector` GIN index
        - Resolution: Deferred — premature for the current corpus size, adds a second extension dependency (`pg_trgm`) to a template that today only needs `ltree`, and index strategy belongs with the retrieval/embedding layer rather than the ingestion DDL. Re-decided 2026-08-21 in `docs/activities/engine/20260820v01_harden_engine_correctness_and_consistency.md` (task 21.2): still deferred, and the rebuild window is NOT the cheapest moment for this one — a `create index if not exists` added to the template executes on every run and reaches pre-existing tables, so the index can land the moment a measured keyword workload exists; today that workload flows through the embedding tables' `chunk_tsv` GIN index. The `pg_trgm` dependency concern is also now sharper: the engine no longer creates extensions (extension contract, task 4), so a trigram index would add a provisioning requirement to every instance.

3. [completed] Cross-file consistency with the reviewed group - `code/excel_ingestion/sql/excel_schema.sql`
   - 3.1. [suggestion] Lines 28-29: `check (n_rows >= 1)` is the only place the "a sheet must have at least one data row" invariant is written down. Its caller-side counterpart — `write_consolidated` in `ingest_excel.py` (424, 487) inserts `n_rows = len(rows)` and therefore requires a non-empty `rows` — is undocumented in that function's docstring; the guard exists only as the `if not rows` skip at `ingest_excel.py:628-632`. See `docs/code_review/excel_ingestion/cr_20260816v01_ingest_excel.md` finding 2.1.
        - Current: the invariant is enforced in the DDL and honoured by an inline caller check, with no contract on the callable
        - Expected: no change here; the fix belongs in `write_consolidated`'s docstring (tracked in the `ingest_excel.py` review)
        - Resolution: Deferred — the SQL side is correct and its header (11-12) already states the defense-in-depth intent; recorded here only so the pairing is visible from both files' reviews.

## Skills with No Issues

1. Formatting (lowercase, 4-space indent): No issues found — all keywords are lowercase, one column definition per line, CHECK clauses wrapped and indented under their column. The line-length half of this rule is finding 1.1.
2. Comments (explain why / business rules): No issues found — the header explains the placeholder-substitution contract and the validate-before-substitute guarantee, the no-migration NOTE (14-18) records the operational caveat raised in the 2026-06-24 review (RESOLVED), the `source_binary_hash` block (30-32) explains the uint64 / `numeric(20,0)` sizing, and the comment block (57-61) explains that `ingest_excel.py`'s config overrides are applied after this template and win.
3. DDL correctness: No issues found — `create extension if not exists ltree` and `create schema if not exists` precede the tables; the parent PK is `collection_path ltree primary key`; the child's FK references `{sheet_table} (collection_path) on delete cascade` with composite PK `(collection_path, sort_order)`. The FK target is the configured `{sheet_table}`, matching the name `structured_table.ensure_table` uses for its own FK (`structured_table.py:125-127`), so all three tables hang off one parent.
4. CHECK constraints: No issues found — `n_rows >= 1`, `source_binary_hash` bounded to `[0, 2^64)`, `sort_order >= 1`, `row_text <> ''`, `word_count >= 0`; each mirrors an application invariant produced by `write_consolidated` (`ingest_excel.py:477-509`).
5. Idempotency: No issues found — `if not exists` on the extension, schema, and both tables; `comment on table` overwrites, so a re-run refreshes the descriptions. The no-ALTER caveat is documented in the header NOTE.
6. Placeholder / substitution soundness: No issues found — exactly the three placeholders (`{schema_name}`, `{sheet_table}`, `{content_table}`) that `ensure_consolidated_tables` substitutes (`ingest_excel.py:322-327`), each validated by `validate_sql_identifier` before substitution and re-checked by the leftover-placeholder guard (331-336); no literal braces in the file that could false-positive that guard.
7. Explicit columns / joins / CTEs / union / group by: N/A — this file is DDL, not a query; there is no SELECT, join, or aggregation.
8. Query block annotation: N/A — the rule applies to queries and CTEs; each DDL statement nevertheless carries a one-line purpose comment above it (24, 42-43, 57-61).
