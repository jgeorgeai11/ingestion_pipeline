---
name: cr_20260816v01_test_ingest_excel
goal: Address coverage and style issues in code/excel_ingestion/unit_tests/test_ingest_excel.py (re-review since 20260626v01, covering the new COMMENT ON key tests and reviewed as a group with ingest_excel.py and sql/excel_schema.sql) to align with the python-development unit-tests skill.
created: 2026-08-16 19:33:40
updated: 2026-08-17 09:53:30
---

## Implementation Plan

1. [completed] Untested `validate_config` guards - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 1.1. [minor] Lines 204-225 (the shape-guard tests): the three `isinstance(..., dict)` guards in `ingest_excel.validate_config` still have no test — non-dict `files` (`ingest_excel.py:152-155`, "must be a table of workbooks"), non-dict file entry (`159-162`, "must be a table"), and non-dict sheet entry (`174-178`, "must be a table"). Their sibling guard is covered (`test_validate_config_sheets_not_a_list_raises`, line 220), so the gap is asymmetric. Carried forward from `20260626v01_cr_test_ingest_excel.md` item 2.1, still open.
        - Current: no test assigns a non-dict to `files`, to `files["wb.xlsx"]`, or to `files["wb.xlsx"]["sheets"][0]`
        - Expected: one parametrized test setting each of `config["files"] = []`, `config["files"]["wb.xlsx"] = "oops"`, `config["files"]["wb.xlsx"]["sheets"][0] = "oops"` and asserting `pytest.raises(ValueError, match="must be a table")`
        - Resolution: Implemented as specified — added `test_validate_config_non_table_entry_raises`, parametrized over the three `(keys, bad_value)` pairs with `ids=["files", "file_entry", "sheet_entry"]`; the test walks `keys` into a fresh `_valid_config()` and assigns `bad_value`, then asserts `pytest.raises(ValueError, match="must be a table")`. Each of the three messages ("must be a table of workbooks", "must be a table") matches, and all three guards now fail the test if removed.
   - 1.2. [minor] Lines 228-233: `test_validate_config_bad_min_column_overlap_raises` exercises only the string case (`"0.5"`), leaving two deliberately-coded branches of the same guard untested — the explicit `isinstance(min_overlap, bool)` rejection (`ingest_excel.py:143`, so `min_column_overlap = true` would otherwise pass as `int` 1) and the range rejection `not 0.0 <= min_overlap <= 1.0` (`145`). The accepted boundaries (`0.0`, `1.0`) are likewise unpinned.
        - Current: `config["min_column_overlap"] = "0.5"` as the single case
        - Expected: `@pytest.mark.parametrize("bad", ["0.5", True, 1.5, -0.1])` on the raising test, plus a `@pytest.mark.parametrize("good", [0.0, 0.5, 1.0])` acceptance test
        - Resolution: Implemented as specified — `test_validate_config_bad_min_column_overlap_raises` is now parametrized over `["0.5", True, 1.5, -0.1]` and tightened its `match` to `"min_column_overlap must be a number"`; added `test_validate_config_accepts_min_column_overlap_bounds` over `[0.0, 0.5, 1.0]`. Both docstrings state why `True` needs the explicit bool rejection (it is an `int` subclass).

2. [completed] The new COMMENT ON key tests do not pin the refresh semantics - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 2.1. [minor] Lines 679-715 (`test_pipeline_comment_keys_reach_the_catalog`): the test proves the overrides reach `pg_catalog` on a first run, but both `ensure_consolidated_tables` (`ingest_excel.py:292-296`) and `structured_table.ensure_table` (`structured_table.py:104-107`) document a stronger contract — the COMMENT ON is applied on EVERY call, not only at creation, so a re-run with new text refreshes the description. Nothing exercises a second run, so an implementation that only commented on first creation (e.g. moved inside a create-time branch) would still pass the suite.
        - Current: a single `_run(config)` followed by four `_object_comment` equality assertions
        - Expected: re-run the pipeline with a second config whose `schema_comment`/`sheet_table_comment`/`content_table_comment`/`table_comment` differ, and assert the catalog now returns the NEW text (proving overwrite-on-re-run, not create-only)
        - Resolution: Implemented as a separate test rather than by extending the existing one, so each test keeps a single behaviour: added `test_pipeline_comment_keys_refresh_on_rerun`, which runs the pipeline twice over the same workbook/collection_path with all four override texts changed ("First ..." then "Second ...") and asserts the catalog returns the second run's text for the schema, both consolidated tables, and the structured table. Both configs are built by a new `_comment_config_toml()` helper (also used by `test_pipeline_comment_keys_reach_the_catalog`, which is otherwise unchanged) that keeps `overwrite = false`, so the second run skips both row-write legs and the assertions can only pass if the COMMENT ON statements ran outside the create path — a create-time-only implementation fails. A closing assertion that `wb.alpha` still has exactly 2 content rows pins that the refresh did not re-write the skipped sheet. Verified against the real ephemeral schema (DB reachable in this run: 66 tests in the file, 0 skipped).
   - 2.2. [suggestion] Lines 679-715 vs 748-837: the comment overrides are only exercised with the default `sheet`/`sheet_content` names, and the custom-name test carries no comment keys — yet `ensure_consolidated_tables` builds `comment on table {db_schema}.{sheet_table}` from the CONFIGURED names (`ingest_excel.py:348-360`), so a bug that commented the hardcoded defaults would go unnoticed.
        - Current: the two features are tested in disjoint tests
        - Expected: add `sheet_table_comment`/`content_table_comment` to the custom-name config in `test_pipeline_custom_consolidated_table_names` and assert `_object_comment(engine, schema, "xls_sheet")` returns the configured text
        - Resolution: Deferred — low incremental value; the comment SQL and the DDL render read `sheet_table`/`content_table` from the same two local variables (`ingest_excel.py:563-564`, threaded at 578-583), and the custom-name test already proves those variables carry the configured names.

3. [completed] No tests for the config type guards flagged in the source review - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 3.1. [minor] Lines 64-274 (the `validate_config` block): the suite pins a guard for every config key that has one — row bounds, column letters, identifiers, comment keys, `min_column_overlap` — but there is nothing for `overwrite`, `source_dir`, or `db_name`, which have no guard at all (see `docs/code_review/excel_ingestion/cr_20260816v01_ingest_excel.md` findings 1.1-1.3: a quoted `overwrite = "false"` is truthy and silently overwrites; a non-string `db_schema`/`source_dir` escapes `main()`'s `ValueError` abort as a `TypeError`). The test-side work lands with that fix.
        - Current: no test asserts a `ValueError` for `overwrite = "false"`, `source_dir = 5`, or `db_name = 5`
        - Expected: after the guards are added, `test_validate_config_non_bool_overwrite_raises` and a parametrized non-string test over `["source_dir", "db_name", "db_schema"]`, each asserting `pytest.raises(ValueError, match="must be a (boolean|string)")`
        - Resolution: Implemented alongside the source guards (`cr_20260816v01_ingest_excel.md` 1.1-1.3), with the coverage extended past the documented list so no new guard branch is untested: `test_validate_config_non_bool_overwrite_raises` (with `overwrite = "false"`, the exact silent-overwrite case) plus `test_validate_config_accepts_bool_overwrite`; `test_validate_config_non_string_top_level_field_raises` parametrized over `["source_dir", "db_name", "db_schema", "sheet_table", "content_table"]` (the two consolidated names added because finding 1.2 put guards there too); and `test_validate_config_non_string_table_raises` for the per-sheet `table`. Each asserts `match=f"{field} must be a string"` / `"overwrite must be a boolean"`.

4. [completed] Docstring style on one test - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 4.1. [minor] Lines 194-196: `test_validate_config_equal_consolidated_table_names_raises` wraps its summary across two physical lines and closes on the body line, unlike every other docstring in the file (one-line summary, or summary + blank line + body as in lines 682-687).
        - Current: `"""sheet_table == content_table is rejected as a config error (would make the\n    second create-if-not-exists a no-op, hiding the missing content columns)."""`
        - Expected: `"""sheet_table == content_table is rejected as a config error.\n\n    An equal pair would make the second create-if-not-exists a no-op, hiding\n    the missing content columns.\n    """`
        - Resolution: Implemented as specified — reflowed to a one-line summary, a blank line, the body, and a closing `"""` on its own line, matching the file's other multi-line docstrings.

5. [completed] Coverage and style items (5.4 promoted; others deferred) - `code/excel_ingestion/unit_tests/test_ingest_excel.py`
   - 5.1. [suggestion] Lines 567-639: two of the four per-sheet resilience stages remain unexercised — the collection_path resolution failure (`ingest_excel.py:636-653`) and the embed-leg `SQLAlchemyError` (`672-684`, which also suppresses the structured leg via `continue`). The parse stage (`test_pipeline_resilient_parse_failure`) and the structured stage (`test_pipeline_resilient_mixed_batch`) are covered. Carried forward from `20260626v01_cr_test_ingest_excel.md` item 3.1.
        - Current: only the parse and structured failure stages have end-to-end tests
        - Expected: a workbook whose sheet name and stem sanitize to an empty ltree label (so `make_collection_path` raises) alongside a good sheet, asserting exit 1 and the good sheet still loaded
        - Resolution: Deferred — the embed-`SQLAlchemyError` half needs fault injection into a real transaction, and the collection_path half needs a workbook with a degenerate (all-punctuation) sheet name; both stages share the same recorded-failure-and-continue code shape already pinned twice.
   - 5.2. [suggestion] Lines 679-745: `ensure_consolidated_tables`' two documented `Raises` conditions are still untested — the missing-DDL `FileNotFoundError` (`ingest_excel.py:317-318`) and the unsubstituted-placeholder `ValueError` (`331-336`). Carried forward from `20260626v01_cr_test_ingest_excel.md` item 4.1.
        - Current: no test points the DDL path at a missing file or at a template containing a stray `{foo}`
        - Expected: monkeypatch the resolved `sql/excel_schema.sql` path to a missing file and to a template with a stray placeholder, asserting each exception
        - Resolution: Deferred — both are drift guards over a file that ships with the module, and testing them requires monkeypatching a path computed inside the function; the happy-path substitution of all three placeholders is already covered end-to-end by the default and custom-name pipeline tests.
   - 5.3. [suggestion] Lines 321-332: `_run` swaps `sys.argv` by hand with a `try/finally` restore rather than using pytest's `monkeypatch` fixture, which the unit-tests skill lists as the built-in for exactly this.
        - Current: `old = sys.argv; sys.argv = argv; try: ... finally: sys.argv = old`
        - Expected: `def _run(config_path: Path, monkeypatch: pytest.MonkeyPatch) -> int:` with `monkeypatch.setattr(sys, "argv", [...])`
        - Resolution: Deferred — the `finally` restore is already exception-safe and order-independent; switching would thread the `monkeypatch` fixture through all ten pipeline tests for no behavioural gain.
   - 5.4. [minor] Lines 282-297: the `build_row_text` tests cover a normal value, `None`, and the `row_number` exclusion, but not a falsy-but-present value — `ingest_excel.py:389` uses `row.get(col) or ''`, so an `int` `0` or `""` (both permitted by the `dict[str, str | int | None]` annotation) renders as empty. See finding 3.1 in the `ingest_excel.py` review. Promoted from suggestion 2026-08-17 (re-rated to minor) together with that source change; the test lands in the same change so it asserts the new `is None` behavior.
        - Current: `test_build_row_text_none_becomes_empty` pins only the `None` case
        - Expected: a case asserting `build_row_text(["A"], {"A": 0, ROW_NUMBER_KEY: 1}) == "A: 0"` once the source uses an `is None` check
        - Resolution: Implemented as specified and slightly broadened — added `test_build_row_text_falsy_but_present_value_survives`, parametrized over `[0, "", "0"]` and asserting `build_row_text(["A"], {"A": falsy, ROW_NUMBER_KEY: 1}) == f"A: {falsy}"`, which covers the `0` case from the Expected plus the empty-string and truthy-`"0"` cases (the latter is what `excel_parser._cell_value` actually produces today). Landed in the same change as the source fix (`cr_20260816v01_ingest_excel.md` 3.1), so it asserts the new `is None` behaviour; `test_build_row_text_none_becomes_empty` is unchanged.
   - 5.5. [suggestion] Lines 738-743: `test_pipeline_template_bakes_generic_comments_without_overrides` asserts the substrings `"Catalog of ingested Excel worksheets"` and `"row-level text"`, coupling the test to the exact wording of `sql/excel_schema.sql:63` and `:66`. The group's schema review (`docs/code_review/excel_ingestion/sql/cr_20260816v01_excel_schema.md` finding 1.1) proposes rewrapping those two over-length literals, which must preserve both substrings within a single literal fragment.
        - Current: `assert "Catalog of ingested Excel worksheets" in sheet_comment` / `assert "row-level text" in content_comment`
        - Expected: keep the substring assertions (they are the right granularity — they prove the template's own text survived, not an override) and note the constraint when rewrapping the DDL
        - Resolution: Deferred — the coupling is intentional and correctly scoped to a stable phrase rather than the full sentence; no test change is needed, only care in the paired DDL edit.

## Skills with No Issues

1. unit-tests (the new comment-key tests are real, not tautological): No issues found — `test_pipeline_comment_keys_reach_the_catalog` reads the descriptions back from `pg_catalog` via `obj_description` on `pg_namespace`/`pg_class` (`_object_comment`, lines 657-676), i.e. the real catalog rather than the config it wrote, and it discriminates the override-wins-over-template ordering because the sheet/content tables get generic text from the template first (`sql/excel_schema.sql:62-66`) and the asserted values are the config's. The companion `test_pipeline_template_bakes_generic_comments_without_overrides` pins the no-override branch, including that the schema description stays `None` (line 745) since the template has no `comment on schema`. The single quote in `"Alpha's structured code list"` (line 704) additionally exercises the bound-parameter quoting path in `structured_table.ensure_table` (`structured_table.py:144-150`).
2. unit-tests (comment validation coverage): No issues found — `test_validate_config_accepts_comment_keys` (236), the parametrized `test_validate_config_non_string_top_level_comment_raises` over all three top-level keys (246-257), `test_validate_config_table_comment_without_table_raises` (260, correctly using `sheets[1]`, the entry with no `table`), and `test_validate_config_non_string_table_comment_raises` (269) map one-to-one onto the guards at `ingest_excel.py:129-136` and `246-257`.
3. unit-tests (naming, AAA, order independence): No issues found — files and functions follow `test_<function>_<scenario>_<expected>`; `_valid_config()` returns a fresh dict per call and `ephemeral_schema` yields a unique UUID schema per test, so no test depends on another's state or on ordering.
4. unit-tests (parametrize / pytest.raises): No issues found — `@pytest.mark.parametrize` is used for the missing-top-level fields (69), the column-letter fields (95), the unsafe consolidated names (183), and the top-level comment keys (246); every negative case uses `pytest.raises(..., match=...)` against a message that exists in the source or the delegated validator (`code/lib/validators.py:40-43`).
5. unit-tests (mock realism): No issues found — the config tests are pure, and the pipeline tests drive real `openpyxl` workbooks through the real `ingest_excel.main()` against a real ephemeral schema; nothing is mocked that would make an assertion tautological, and `conftest.py`'s `ephemeral_schema` skips cleanly when the DB is unreachable while dropping the schema with CASCADE in a `finally`.
6. unit-tests (assertion strength on the default path): No issues found — the persisted `row_text`/`word_count` (403-404), the recomputed `source_binary_hash` with a non-zero check (446-452), the default title `"wb — Alpha"` (443), and `after == before` for the skip-is-not-a-re-insert case (506) each discriminate a plausible wrong implementation.
7. type-hints: No issues found — every test and helper (`_valid_config`, `_make_workbook`, `_run`, `_write_config`, `_content_rows`, `_sheet_row`, `_table_exists`, `_object_comment`) annotates parameters and returns with modern syntax (`dict | None`, `tuple[Engine, str]`, `str | None`).
8. docstrings: Every test and helper carries a behaviour docstring; the one style deviation is finding 4.1.
9. logging: No issues found — `setup_logging(log_dir="logs/excel_ingestion/unit_tests", log_name="test_ingest_excel")` at line 23 mirrors the module path per the logging skill's directory table, and it runs before the modules under test are imported.
10. sql-development: No issues found — the read-back helpers use lowercase SQL, bind every value (`:cp`, `:s`, `:t`), and use `inner join` explicitly in `_object_comment` (671-673). The `select *` in `_content_rows`/`_sheet_row` is test introspection of a just-written row, not a query over raw inputs.
11. Data Validation: N/A — this is a unit-test module; the `data_val_` scripts are reviewed separately.
