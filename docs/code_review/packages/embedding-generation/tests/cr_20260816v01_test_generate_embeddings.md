---
name: cr_20260816v01_test_generate_embeddings
goal: Address code quality issues identified in code/embedding_generation/unit_tests/test_generate_embeddings.py to align with the python-development unit-tests skill; re-review since cr_20260625v01, reviewed jointly with generate_embeddings.py.
created: 2026-08-16 19:35:47
updated: 2026-08-17 09:54:23
---

## Implementation Plan

1. [completed] Suite cannot be collected under the project's own pytest configuration - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 1.1. [major] Whole file: With the `addopts = "--import-mode=importlib"` configured in `pyproject.toml`, pytest derives module names from the rootdir-relative path, so importing this file first imports the repo-root `code/` directory as a namespace package named `code`. That shadows the standard-library `code` module, and the subsequent `import pdb` (performed by pytest's own `debugging` plugin and by `torch.distributed`) fails with `AttributeError: module 'code' has no attribute 'InteractiveConsole'` on the project venv's Python 3.14.3. Reproduced from the repo root: `uv run pytest code/embedding_generation/unit_tests/test_generate_embeddings.py -q` → `INTERNALERROR ... AttributeError`, while `uv run pytest code/embedding_generation/unit_tests/test_generate_embeddings.py -q --import-mode=prepend` → `64 passed`. `test_chunker.py` in the same directory fails identically, so the suite currently provides no assurance on a supported interpreter (`requires-python = ">=3.13"`).
        - Current: `[tool.pytest.ini_options] addopts = "--import-mode=importlib"` with the tests under a top-level `code/` directory and no package-name isolation.
        - Expected: make collection interpreter-safe — e.g. add `consider_namespace_packages`/`pythonpath` handling or a root `conftest.py` that imports the stdlib `code` module before pytest's rootdir-relative import runs, or keep `importlib` mode but stop the rootdir-relative name from resolving to `code` (the `--import-mode=prepend` run already passes all 64 tests, so no test change is required to prove the suite green).
        - Resolution: Already fixed — no change needed in this file. The repo-root `conftest.py` created for the same finding in the sibling `file_ingestion`/`excel_ingestion` reviews resolves it repo-wide by pre-importing the stdlib `pdb` (and therefore `code.InteractiveConsole`) while the stdlib `code` module is still intact, before pytest's importer swaps `sys.modules["code"]` for this repo's namespace package. Importing `code` alone is insufficient, as that conftest's docstring records. `--import-mode=importlib` is retained (the `prepend` alternative aborts on the `test_utils.py` basename collision). Verified on the venv's Python 3.14.3: `uv run pytest code/embedding_generation/unit_tests/test_generate_embeddings.py -q` → 64 passed before this round's test additions, 71 passed after; the whole directory (with `test_chunker.py`) → 91 passed.

2. [completed] Uncovered error branches of the module under test - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 2.1. [minor] Lines 270-311 (`TestGetEngine`): The non-integer `POSTGRES_PORT` branch (`generate_embeddings.py:106-109`) is untested. That conversion guard was added in response to cr_20260625v01 finding 3.1, and it is the one `_get_engine` failure mode with no test; the neighbouring missing-env-var path is covered.
        - Current: three tests — missing env vars, valid env vars, percent-encoded credentials.
        - Expected: `test_get_engine_non_integer_port_raises` setting `POSTGRES_PORT="abc"` and asserting `pytest.raises(ConfigurationError, match="POSTGRES_PORT is not an integer")`.
        - Resolution: Implemented as specified — `test_get_engine_non_integer_port_raises` sets all four `POSTGRES_*` vars via `monkeypatch` (the other three valid, so the missing-env-var guard is passed and the port conversion is what fails) and asserts `ConfigurationError` matching "POSTGRES_PORT is not an integer".
   - 2.2. [minor] Lines 1298-1426 (`TestMain`): Two `main()` config-error exits are untested — the malformed-TOML handler (`generate_embeddings.py:743-745`, `except (tomllib.TOMLDecodeError, OSError)`) and the empty-`tables` guard (`generate_embeddings.py:757-759`). `TestMain` covers the missing-file and missing-field exits, so these two are the remaining gaps in the same branch family.
        - Current: no test writes invalid TOML or a config whose `tables` table is empty.
        - Expected: `test_main_invalid_toml_exits_one` (write `not = valid = toml`) and `test_main_no_tables_exits_one` (header only, no `[tables.*]`), each asserting `SystemExit(1)`.
        - Resolution: Implemented with one correction to the documented fix. `test_main_invalid_toml_exits_one` is exactly as specified (`not = valid = toml` → `TOMLDecodeError` → `SystemExit(1)`). For `test_main_no_tables_exits_one` the "header only, no `[tables.*]`" body would have hit the *missing-required-field* `KeyError` branch (already covered by `test_main_missing_required_field_exits_one`) rather than the empty-`tables` guard, so the config body is `_BASE_HEADER + "[tables]\n"` — an empty `[tables]` table, which is what the source review's finding 5.1 describes and what makes `if not tables:` the branch under test. Both tests also assert `generate_embeddings` was never called, pinning the exit as pre-processing.
   - 2.3. [minor] Lines 475-497: `test_get_embedding_model_loads_and_caches` patches `generate_embeddings.torch` wholesale, so `torch.backends.mps.is_available()` returns a truthy `MagicMock` and the device is always `"mps"`; the assertion is only `mock_st.assert_called_once()`, so neither device branch (`generate_embeddings.py:70`) is actually verified.
        - Current: `mocker.patch("generate_embeddings.torch")` then `mock_st.assert_called_once()`.
        - Expected: assert the constructor arguments, e.g. `mock_st.assert_called_once_with("test-model", device="mps")`, plus a companion case setting `mock_torch.backends.mps.is_available.return_value = False` and asserting `device="cpu"`.
        - Resolution: Implemented as specified — `test_get_embedding_model_loads_and_caches` now binds the `torch` patch to `mock_torch`, sets `backends.mps.is_available.return_value = True` explicitly (rather than relying on a truthy `MagicMock`), and asserts `mock_st.assert_called_once_with("test-model", device="mps")`, which checks caching and the device in one call. The new companion `test_get_embedding_model_falls_back_to_cpu_without_mps` sets `is_available` to `False` and asserts `device="cpu"`, so both branches of line 70 are now verified.

3. [completed] Type hints on module-level test helpers - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 3.1. [major] Lines 808-814: `_setup_generate_embeddings_mocks` has no return type annotation even though it returns a two-tuple that callers unpack (line 904), and the parameters use bare `list`. Every test method in the file is annotated `-> None`, so this helper is the sole unannotated function.
        - Current: `def _setup_generate_embeddings_mocks(mocker, chunk_return: list, insert_calls: list, token_count: int = 4, source_rows: list | None = None,):`
        - Expected: annotate the return (`-> tuple[MagicMock, MagicMock]`) and parameterize the containers, e.g. `chunk_return: list[tuple[dict[str, object], int]]`, `insert_calls: list[dict[str, object]]`, `source_rows: list[tuple] | None = None`.
        - Resolution: Implemented with one substitution in the annotation type. The containers are parameterized exactly as documented (`list[tuple[dict[str, object], int]]`, `list[dict[str, object]]`, `list[tuple] | None`), but the return is `-> tuple[MockType, MockType]` using `from pytest_mock import MockType` rather than `MagicMock` from `unittest.mock`: this suite is pytest-mock-only (recorded under "Skills with No Issues" item 1 as "no `unittest.mock` imports remain"), and `MockType` is pytest-mock's own public alias for the same object (`MagicMock | AsyncMock | NonCallableMagicMock`), so the annotation is equally specific without re-introducing a `unittest.mock` import. The two new connection accessors from finding 6.2 use the same `MockType` return.
   - 3.2. [minor] Line 1283: `_write_config_text(tmp_path, body: str) -> str` leaves `tmp_path` unannotated while its sibling parameter is typed; the fixture value is a `pathlib.Path`, not an un-typeable pytest fixture object.
        - Current: `def _write_config_text(tmp_path, body: str) -> str:`
        - Expected: `def _write_config_text(tmp_path: Path, body: str) -> str:` with `from pathlib import Path` at module scope.
        - Resolution: Implemented as specified — `from pathlib import Path` added at module scope and the parameter annotated `tmp_path: Path`.

4. [completed] Docstring accuracy and currency - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 4.1. [minor] Lines 815-833: `_setup_generate_embeddings_mocks`'s docstring says "The batch INSERT params are captured into `insert_calls`", but the `side_effect` is attached to the whole `engine.begin()` connection, so it also captures the overwrite DELETE's bound params (`generate_embeddings.py:549`). In `test_overwrite_deletes_then_regenerates` the first entry of `insert_calls` is therefore the DELETE's `{"sf_module_name_0": "Pub%"}`, not an INSERT — a trap for the next test that asserts on `insert_calls[0]` in an overwrite scenario. The `Args` section also omits the `source_rows` parameter entirely.
        - Current: `def _capture_insert(_sql, params=None): if params is not None: insert_calls.append(params)` documented as capturing "the batch INSERT params"; `Args:` lists only `mocker`, `chunk_return`, `insert_calls`, `token_count`.
        - Expected: either filter on the statement (`if "insert into" in str(_sql)`) so the name matches the behavior, or document that every parameterized statement on the write connection is captured; and add the missing `source_rows` entry to `Args`.
        - Resolution: Implemented via the filtering option (the trap is removed rather than documented) — `_capture_insert` now appends only when `"insert into" in str(_sql)`, with a comment naming the overwrite DELETE as the statement being excluded, so `insert_calls[0]` is the first INSERT in every scenario including `test_generate_embeddings_overwrite_deletes_then_regenerates`. The docstring records the filtering, and `source_rows` was added to `Args` (documenting that rows are tuples in `column_names` order and that None yields a single default row). No existing assertion changed meaning: the overwrite test asserts on the write connection's DELETE calls directly, not on `insert_calls`.
   - 4.2. [minor] Lines 1-6: The module docstring's coverage list is stale relative to the suite — it omits the contextual chunk headers (`TestContextualChunkHeaders`), the `COMMENT ON` resolution (`TestEmbeddingTableComment`), the `main()` entry point (`TestMain`), the token-counter wiring (`TestTokenCounter`), and the resilience branches (`TestGenerateEmbeddingsResilience`), all of which were added after the docstring was written.
        - Current: `Tests config parsing, dynamic SQL generation, PK auto-detection, identifier validation, embed text building, source table verification, engine creation, embedding model caching, and end-to-end generation.`
        - Expected: extend the list to name the header, table-comment, `main()`, token-counter, and resilience areas.
        - Resolution: Implemented as specified — the module docstring's coverage list now names contextual chunk headers, the resolved COMMENT ON table description, token-counter wiring, the resilience branches (source-filter validation, overwrite, no-rows, row-boundary batching), and the `main()` entry point, alongside the pre-existing areas. Also added "device selection" to the model-caching entry to match the new branch tests from finding 2.3.

5. [completed] Test naming convention - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 5.1. [minor] Lines 32, 36, 670, 890, 941, 977, 1014, 1070, 1086, 1101, 1115, 1169, 1217, 1248: These newer tests drop the `test_<function>_<scenario>_<expected>` function-name prefix that cr_20260625v01 established for the older classes, so the file now mixes both conventions (`test_build_source_filter_clause_with_table_alias` next to `test_no_rows_returns_zero`).
        - Current: `test_accepts_safe_identifier`, `test_rejects_trailing_newline`, `test_source_filter_reaches_select_and_binds_params`, `test_header_prepended_to_every_chunk_including_tail`, `test_no_rows_returns_zero`, `test_overwrite_deletes_then_regenerates`, `test_default_comment_with_header_columns`, ...
        - Expected: prefix with the function under test, e.g. `test_validate_sql_identifier_accepts_safe_identifier`, `test_generate_embeddings_no_rows_returns_zero`, `test_generate_embeddings_overwrite_deletes_then_regenerates`.
        - Resolution: Implemented as specified for all 14 listed tests — the two `TestValidateSqlIdentifier` tests took the `test_validate_sql_identifier_` prefix, and the twelve driven through the public entry point took `test_generate_embeddings_` (header, table-comment, source-filter, overwrite, no-rows, and batching cases). Scenario/expectation suffixes were left untouched. One test beyond the list was renamed for consistency: `test_source_filter_unknown_column_raises` → `test_generate_embeddings_source_filter_unknown_column_raises`, since it sits in the same `TestGenerateEmbeddingsResilience` class as two listed tests and would otherwise have been the only unprefixed name left in the file. The whole file now follows `test_<function>_<scenario>_<expected>`.

6. [completed] Helper placement and duplicated mock plumbing - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 6.1. [minor] Lines 808 vs 608: `_setup_generate_embeddings_mocks` is defined ~200 lines below its first call site, so a reader hitting `TestDynamicSqlGeneration` has to scroll past four classes to learn what the harness stubs.
        - Current: helper defined at line 808, first used at line 608.
        - Expected: move `_setup_generate_embeddings_mocks` above `TestDynamicSqlGeneration` (next to `_captured_select_sql` / `_executed_sql_texts` at lines 579-596), or promote it to a `conftest.py` fixture factory shared with future suites.
        - Resolution: Implemented via the move (not the conftest promotion — the harness encodes this module's specific mock shape and has no second consumer). `_setup_generate_embeddings_mocks` now sits immediately after the accessor helpers and directly above `TestDynamicSqlGeneration`, so it is defined before its first call site and reads as one block with `_select_connection` / `_write_connection` / `_captured_select_sql` / `_captured_select_params` / `_executed_sql_texts`.
   - 6.2. [minor] Lines 701-704, 1200-1202, 1224-1226, 1243-1245: Four sites re-inline the `_get_engine.return_value.connect/begin.return_value.__enter__.return_value` chain even though `_captured_select_sql()` and `_executed_sql_texts()` (lines 579-596) exist for exactly that purpose, so the mock-shape knowledge is duplicated five ways and every one of them breaks if the harness changes.
        - Current: `select_conn = (generate_embeddings_module._get_engine.return_value.connect.return_value.__enter__.return_value)` repeated per test.
        - Expected: add a `_write_connection()` accessor alongside the existing helpers and route all four sites through `_captured_select_sql()` / `_write_connection()`.
        - Resolution: Implemented as specified, with the accessor pair generalized one step further. Added `_write_connection()` and, symmetrically, `_select_connection()`, and rewrote `_captured_select_sql()` / `_executed_sql_texts()` on top of them, so the two mock-chain expressions now appear exactly once each in the file (verified by grep for `__enter__.return_value`: two hits, both accessor bodies). All four duplicated sites were rerouted: the source-filter test uses `_captured_select_sql()` plus a new `_captured_select_params()` (needed because it asserts on the bound params too, which no existing helper exposed), the overwrite test filters `_write_connection().execute.call_args_list`, and the no-rows test uses `_select_connection()` to stub the empty fetch and `_write_connection()` for `assert_not_called()`.

7. [completed] Brittle assertion and leaked module state - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 7.1. [minor] Line 1068: `_resolved_comment` reads the `table_comment` argument by positional index (`call_args[0][6]`), which silently starts asserting on the wrong argument if a parameter is ever inserted into `_create_embedding_table`'s signature ahead of it.
        - Current: `return generate_embeddings_module._create_embedding_table.call_args[0][6]`
        - Expected: bind by name, e.g. `sig.bind(*call_args.args, **call_args.kwargs).arguments["table_comment"]`, or have the source call `_create_embedding_table(..., table_comment=table_comment)` so `call_args.kwargs["table_comment"]` is available.
        - Resolution: Implemented via the signature-binding option, keeping the change inside the test. `_resolved_comment()` binds the recorded call against `inspect.signature(_create_embedding_table)` — the module-level import at the top of the suite still references the real, unpatched function, so the signature is authoritative — and reads `.arguments["table_comment"]`. The source call site was deliberately left positional: it is correct as written, and the test no longer depends on argument order either way. The docstring records why the binding is done by name.
   - 7.2. [minor] Lines 480-481, 505-506: `TestGetEmbeddingModel` assigns the module globals `_model`/`_model_name` directly and never restores them, so both tests leave a `MagicMock` installed as the process-wide model cache for every test that runs afterwards. It happens to be harmless today only because every other test patches `_get_embedding_model` itself.
        - Current: `generate_embeddings._model = None` / `generate_embeddings._model_name = None` at the top of each test, with no teardown.
        - Expected: use `monkeypatch.setattr(generate_embeddings, "_model", None)` (and `_model_name`), which restores the original values after each test.
        - Resolution: Implemented as specified — both original tests (and the new CPU-device test from finding 2.3) take the `monkeypatch` fixture and reset `_model` / `_model_name` with `monkeypatch.setattr`, so the real globals are restored on teardown and no `MagicMock` survives as the process-wide model cache. A comment at the first site records why the direct assignment was unsafe.

8. [completed] Cross-file consistency with `generate_embeddings.py` - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 8.1. [minor] Lines 270-311 and 1298-1426: The source module answers "bad configuration" uniformly — every malformed input produces a `ConfigurationError` or a logged `sys.exit(1)` — but this suite asserts that contract for only some of the inputs (missing env var, missing config file, missing required field, missing `embed_columns`, unknown filter column, non-string `table_comment`) and not for its siblings (non-integer `POSTGRES_PORT`, malformed TOML, empty `tables`). The same gap is recorded as finding 5.1 in the review of `generate_embeddings.py`; closing it there and here should be one change.
        - Current: coverage of the error contract is split — six of nine config-error branches asserted.
        - Expected: complete the family with the three tests named in findings 2.1 and 2.2, so every documented config failure of `generate_embeddings.py` has a matching test.
        - Resolution: Implemented as one change with the source review's finding 5.1 — the three tests from findings 2.1/2.2 close the named gap (non-integer `POSTGRES_PORT`, malformed TOML, empty `tables`), so all nine of the original config-error branches are now asserted. The family grew by two in the same round because finding 4.1 of the source review added two guards: `test_main_tables_not_a_table_exits_one` (a `tables` array exits 1 instead of raising `AttributeError`) and `test_main_table_config_not_a_table_is_skipped` (a scalar per-table entry is recorded as a failed table while the valid table still runs). Coverage of `generate_embeddings.py` from this suite is now 99%, the only unexecuted statement being the `if __name__ == "__main__"` call.

9. [completed] Test-structure enhancements (9.5 promoted; others deferred) - `code/embedding_generation/unit_tests/test_generate_embeddings.py`
   - 9.1. [suggestion] Lines 45-73, 79-141: `TestBuildEmbedText` and `TestBuildSourceFilterClause` run the same call shape over different inputs, which `@pytest.mark.parametrize` expresses in one test each.
        - Current: five and ten near-identical test methods.
        - Expected: `@pytest.mark.parametrize("row, columns, expected", [...])` over a single `test_build_embed_text_*` body.
        - Resolution: Deferred — optional; the current one-behavior-per-method form gives clearer failure names and each test carries its own explanatory docstring, which a parametrized id list would lose.
   - 9.2. [suggestion] Lines 610-621, 643-654, 687-699, 908-920, 956-968, 992-1004, 1029-1041, 1155-1167, 1186-1198, 1229-1240, 1264-1275: Eleven call sites repeat the same ten `generate_embeddings(...)` keyword arguments, differing only in `embed_columns`/`header_columns`/`overwrite`/`source_filter`. `TestEmbeddingTableComment._COMMON` (lines 1053-1063) already demonstrates the shared-kwargs pattern.
        - Current: the full kwargs block repeated per test.
        - Expected: a module-level `_COMMON_KWARGS` dict (or fixture) spread with `**`, overridden per test.
        - Resolution: Deferred — optional readability refactor; the explicit blocks keep each test self-contained and readable in isolation, and the duplication is inert (no behavior depends on it).
   - 9.3. [suggestion] Lines 123, 137, 904: Bindings that are never asserted on — `params` in `test_build_source_filter_clause_single_element_list_no_parens` and `..._alias_with_multiple_columns`, and `mock_chunk` in `test_header_prepended_to_every_chunk_including_tail`.
        - Current: `clause, params = _build_source_filter_clause(...)` / `mock_model, mock_chunk = _setup_generate_embeddings_mocks(...)` with the second name unused.
        - Expected: discard with `_` (or assert on the value, which for the two `params` cases is a cheap extra check).
        - Resolution: Deferred — cosmetic; the unused names cause no failures and the tuple-unpacking form documents what the call returns.
   - 9.4. [suggestion] Lines 954, 990, 1027: These tests reach for the patched attribute (`generate_embeddings_module.chunk_long_sections`) instead of using the `mock_chunk` the harness already returns, so the helper's documented return value goes unused in exactly the tests that need it.
        - Current: `_setup_generate_embeddings_mocks(...)` (return discarded) followed by `mock_chunk = generate_embeddings_module.chunk_long_sections`.
        - Expected: `_, mock_chunk = _setup_generate_embeddings_mocks(...)`.
        - Resolution: Deferred — stylistic; both expressions resolve to the same mock object, so the assertions are equally valid today.
   - 9.5. [minor] Line 1294: `config_path.write_text(body)` writes with the platform's locale encoding (cp1252 on this Windows host), while `tomllib` requires UTF-8 — non-ASCII in a future config body would fail only on Windows. Promoted from suggestion 2026-08-17 (re-rated to minor), matching the identical pending minor in `cr_20260816v01_test_utils.md` 3.2; one argument at one call site.
        - Current: `config_path.write_text(body)`
        - Expected: `config_path.write_text(body, encoding="utf-8")`
        - Resolution: Implemented as specified — `config_path.write_text(body, encoding="utf-8")`, with a comment noting that `tomllib` requires UTF-8 while `write_text` would otherwise use the platform locale encoding. Matches the fix applied for the identical finding in `cr_20260816v01_test_utils.md` 3.2.

## Skills with No Issues

1. unit-tests — pytest, not unittest: No issues found. `pytest`/`pytest-mock` throughout; no `unittest.mock` imports remain.
2. unit-tests — File naming: No issues found. `test_generate_embeddings.py` matches `generate_embeddings.py`, and the suite lives in `unit_tests/` beside the module.
3. unit-tests — Function naming: Issues found — see task 5.
4. unit-tests — Fixtures / structure: No issues found beyond the helper-placement point in task 6. Tests follow Arrange-Act-Assert, use `tmp_path` and `monkeypatch` where appropriate, and `conftest.py` correctly limits itself to `sys.path` setup plus the `_utils` cache eviction (no fixtures to review).
5. unit-tests — Mock external boundaries only: No issues found. The engine, connections, `inspect`, `SentenceTransformer`, `torch`, the tokenizer, and the chunker are patched at their use site in `generate_embeddings`; no real model is loaded and no database is touched.
6. unit-tests — pytest utilities: Parametrize is unused (deferred suggestion 9.1); `pytest.raises(..., match=...)` is used correctly on every error path; no DataFrame comparisons apply.
7. unit-tests — Behavior independence: Issues found — see finding 7.2 (module-global mutation without teardown). Otherwise no test depends on execution order or on another test's state.
8. unit-tests — Coverage: Issues found — see task 2. The revision's headline behaviors are well covered: production SELECT/INSERT text (`TestDynamicSqlGeneration`, closing cr_20260625v01 findings 1.1-1.2), `main()` per-table resilience and CLI override (`TestMain`, closing 2.1), `_resolve_pg_column_type`'s body (closing 3.1), the filter/overwrite/no-rows branches (closing 4.1-4.2), the `get_tokenizer` fallback (closing 5.1), and the new row-boundary batching and `COMMENT ON` resolution.
9. type-hints: Issues found — see task 3. All 64 test methods are annotated `-> None`.
10. docstrings: Issues found — see task 4. Module, every class, every test method, and both module-level helpers carry docstrings.
11. comments: No issues found. The column-order comment in `_setup_generate_embeddings_mocks` and the module-level note on why `_utils` is imported at collection time both explain non-obvious "why".
12. exception-handling: N/A — the suite asserts on exceptions rather than handling them.
13. logging: N/A — test module; `setup_logging` is patched in `TestMain` so no log files are created.
14. executable-scripts: N/A — test module.
15. data-validation: N/A — not a `data_val_` script.
16. sql-development: N/A for this file — the SQL assertions here are substring checks against the production strings built in `generate_embeddings.py`, which is reviewed separately.
